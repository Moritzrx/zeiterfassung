# wessamedia Zeit – Zeiterfassungs-App

Desktop-App (Electron) zur Zeiterfassung für drei Personen: Moritz (Windows), Filipo und Leon (Mac).
Der vollständige Auftrag steht in `docs/auftrag.md`. Diese Datei hält Entscheidungen und den Stand fest.

## Wichtig für die Zusammenarbeit
- Der Auftraggeber (Moritz) ist kein Entwickler. Alles auf Deutsch, jeden Schritt in einfachen Worten erklären, genau sagen, was er wo klicken muss. Nach jedem Schritt eine Testanleitung.
- Nutzungslimit: Das Claude-Konto wird von drei Personen geteilt. Keine Agenten-Schwärme, keine großen Workflows. Arbeit im Hauptkontext erledigen.
- Regelmäßig committen, deutsche Commit-Nachrichten. Alle Texte in der App auf Deutsch, Anrede "du".
- Große Bash-Befehle mit vielen Heredocs scheitern in dieser Umgebung. Dateien mit dem Write-Tool schreiben.

## Technik (fest vorgegeben)
Electron 44 + electron-vite 5 (Vite 7, nicht 8), React 19, TypeScript 5.9, Tailwind CSS 4, Recharts, lucide-react, simple-icons, get-windows, powerMonitor, Supabase (Postgres + Auth), lokaler Zwischenspeicher als JSON-Datei (kein SQLite), electron-builder. Zeitzone Europe/Berlin.

Node.js 24.20 liegt unter `C:\Program Files\nodejs`. In der Bash vor jedem npm-Befehl: `export PATH="/c/Program Files/nodejs:$PATH"`.
Electron 44 bringt Node 24.20 mit, damit funktioniert `require()` auch für reine ESM-Pakete wie get-windows.

## Getroffene Entscheidungen (Stand 8. September 2026)
- Projektordner bewusst außerhalb von OneDrive: `C:\Users\mouga\Projekte\zeiterfassung`.
- App-Name "wessamedia Zeit", Kennung `com.wessamedia.zeit`. Nicht mehr ändern (Datenordner, Autostart, Mac-Berechtigungen hängen daran).
- macOS-Berechtigung "Bildschirmaufnahme" wird seit 11. September 2026 ANGEFORDERT (Entscheidung des Auftraggebers: "auf dem Mac muss man sehen, ob es YouTube oder Sheets ist"; bis dahin bewusst weggelassen). `aktivesFenster` fragt get-windows nur mit `screenRecordingPermission: true`, wenn `systemPreferences.getMediaAccessStatus('screen')` "granted" liefert (`bildschirmrechtErteilt`), sonst weiter mit `false`: mit eingeschalteter Prüfung und fehlender Berechtigung wirft get-windows, und die Erfassung stünde still. Die Systemabfrage löst `bildschirmrechtAnfragen()` (erfassung.ts) einmalig aus (ein Aufruf mit `true`, Fehler erwartbar), IPC `system:bildschirmrechtAnfragen` öffnet danach die Seite `x-apple.systempreferences:com.apple.preference.security?Privacy_ScreenCapture`; `SystemInfo.bildschirmrecht` (`Bildschirmrecht`: nicht-noetig, erteilt, offen, fehlt) steht unter Einstellungen → System → "Fenstertitel (Bildschirmaufnahme)" mit Knopf "Berechtigung anfragen". Nach dem Erteilen muss die App neu gestartet werden (TCC). Weil die App nur ad-hoc signiert ist, kann macOS die Berechtigung nach jedem Update erneut verlangen (Schalter aus/ein). `accessibilityPermission` bleibt false. Auf dem Mac NOCH NIE getestet.
- Bewertung hat vier Werte: produktiv, unproduktiv, ungeklaert, inaktiv. NICHT AM RECHNER = UNPRODUKTIV (10. September 2026, Entscheidung des Auftraggebers gegen Claudes Rat, weil die App nur den Rechner sieht und Handy oder Sofa sonst unsichtbar blieben): Zeit ohne Eingabe (ab der Untätigkeits-Schwelle, rückwirkend ab dem letzten Tastendruck) wird seit 1.0.19 als `unproduktiv` mit `programm null` gespeichert (`src/shared/ruhe.ts`: `istRuhe`, `RUHE_NAME` "Nicht am Rechner"), nicht mehr als neutrales `inaktiv`. ABWESEND = BLAU (gleicher Tag, "wenn ich schlafen gehe, sind 85 % unmöglich, muss realistisch sein", dann "länger als 90 Minuten weg soll leuchtend blau sein, wie Grün bei produktiv"): dauert die Abwesenheit seit dem letzten Tastendruck länger als `ABWESEND_MS` (90 min), setzt `inaktivVerarbeiten` den ganzen Block auf Bewertung `inaktiv` zurück (Rot wird erstattet), Zustand `abwesend`, der Block läuft blau weiter, solange der Rechner wach ist, höchstens `MAX_INAKTIV_MS` (12 h) ab Abwesenheitsbeginn, Mitternachtsteilung behält Blau; danach nichts mehr bis zur nächsten Eingabe; lange Abwesenheit beendet auch den Fokus. Die Bewertung `inaktiv` heißt in der Oberfläche jetzt überall "Abwesend" (`ABWESEND_NAME`), Farbe `--color-abwesend` #38BDF8 mit Leuchten (styles.css, Selektoren wie bei Rot), Kopfzeile "Abwesend seit 23:10, zählt nicht" mit blauem Punkt, Tages-Ring blau statt Grau; alte graue Blöcke sind damit automatisch blau. Kein Datenbank-Skript nötig, weil der Wert `inaktiv` schon existiert. LÜCKEN WERDEN GEFÜLLT (gleicher Tag, "sobald der Rechner zugeklappt wird, blau, bis er wieder läuft"): `lueckeFuellen(von, bis)` in erfassung.ts trägt Zeit ohne Aufzeichnung nach, sobald die App weitermacht: ab der Untätigkeits-Schwelle rot "Nicht am Rechner", ab 90 Minuten blau "Abwesend", an Mitternacht geteilt, höchstens `LUECKE_MAX_MS` (7 Tage) zurück. Ausgelöst von `weiter()` nach Ruhezustand oder Sperrbildschirm (`unterbrochenUm` aus `unterbrechen()`), von der Lückenprüfung in `verarbeiten` (Takt-Abstand > 30 s) und von `start()` ab dem Ende des letzten bekannten Blocks (`speicher.letztesEnde()`, also auch nach "App aus" oder "Rechner aus"). Die Pause-Funktion füllt nichts (bewusst neutral). Regeln und Kurzwechsel fassen Blöcke ohne Programm nicht an (`frei` in bewertung.ts), der Fokus auch nicht. Wer unterwegs gearbeitet hat, trägt es unter "Eintragen" nach; die Überschneidungsprüfung bietet an, die roten Blöcke zu löschen. Kopfzeile und Gerade-Karte sagen "Nicht am Rechner … zählt als unproduktiv". FOKUS-QUOTE (`fokusQuote` in ruhe.ts) = produktiv / (produktiv + unproduktiv + ungeklärt); Auszeichnung "Fokus-Woche" = abgeschlossene Woche auf Zielrang mit Quote ≥ `FOKUS_QUOTE_ZIEL` (0,8; erst 0,85, vorher "höchstens 2 h unproduktiv"); Abwesenheit (blau) zählt in der Quote nicht mit; die Quote steht auf der Woche unter der Restlaufzeit, in der Wochenzusammenfassung statt "Unproduktiv" und als Kachel im Wochenrückblick.
- RÄNGE statt Level (Entscheidung des Auftraggebers vom 9. September 2026, ersetzt das Level-System des Auftrags): Rang 1–10 alle 5 produktive Stunden (Rang 10 = 50 h = Wochenziel), Rang 11–15 alle 2 Stunden (Rang 15 = 60 h), darüber nichts. Namen in `RANG_NAMEN` (`src/shared/rang.ts`), auf Wunsch englisch und episch: Unranked, Recruit, Initiate, Vanguard, Warden, Sentinel, Paladin, Conqueror, Warlord, Overlord, Champion, Titan, Legend, Mythic, Immortal, Eternal. Übersicht aller Ränge als Fenster `RangUebersicht` (Knopf unter dem Ring auf der Woche und "Alle Ränge" im Team). Stufen: Bronze 1–3, Silber 4–6, Gold 7–9, Champion 10 (Orange), Diamant 11–15. Abzeichen sind seit der vierten Runde KI-Wappen (siehe Design), darüber Zahl, Sterne und Namensband als SVG (`RangAbzeichen.tsx`). Das Gesamtziel (Standard 50) bestimmt nur den Ziel-Rang und die Tagesrichtwert-Linie. Das Wort "Level" kommt in der App nicht mehr vor.
- LIGA (Wunsch des Auftraggebers vom 9. September 2026, "wie in Clash of Clans"): Langzeit-Stand über alle Wochen, unabhängig vom Wochenrang. Trophäen je abgeschlossener Woche = clamp(round((produktive Stunden − (Gesamtziel − 10)) × 10), −120, +200); Wochen ohne einen einzigen Block zählen nicht; Start 0 ("Ohne Liga", Entscheidung vom 9. September 2026, vorher 400; Skript `10_liga_start_null.sql`, Skripte 08/09 ebenfalls auf 0 gesetzt); gezählt ab Montag 2026-09-07. Ligen mit Clash-Schwellen (Bronze 400/500/600, Silber 800/1000/1200, Gold 1400/1600/1800, Kristall 2000/2200/2400, Meister 2600/2800/3000, Champion 3200/3500/3800, Titan 4100/4400/4700, Legende 5000). Die Summe rechnet die Datenbankfunktion `liga_stand()` (`supabase/08_liga.sql`, security definer, nur Summen), die Formel steht doppelt in `src/shared/liga.ts` (Vorschau der laufenden Woche, Ligastufen, Farben). IPC `liga:stand`; Fenster `LigaKarte` (Woche, mit Vorschau "Diese Woche bisher" und Auf-/Abstiegs-Hinweis über localStorage `liga.zuletzt.<uid>`), `LigaRangliste` (Team, oben), `LigaAbzeichen` (KI-Wappen je Stufe mit römischer Ziffer, siehe Design), `LigaUebersicht`. Skript 8 hat der Auftraggeber am 9. September 2026 ausgeführt (alle drei bei 400). Die Liga läuft dauerhaft, nie eine Rücksetzung (bestätigt vom Auftraggeber). NIE UNTER 0 (9. September 2026, weil die Karte am Mittwoch "−120" zeigte und der Auftraggeber es nicht verstand): `supabase/12_liga_nicht_unter_null.sql` rechnet Woche für Woche mit Untergrenze 0 (laufende Summe minus tiefstes Zwischenergebnis, Fensterfunktionen) und liefert zusätzlich `letztes_wirksam` (was die letzte Woche am Stand wirklich änderte); `wirksamesDelta` in `src/shared/liga.ts` macht dasselbe für die Vorschau. `LigaKarte` zeigt rechts die wirksame Zahl mit Einheit "Trophäen", darunter "bisher X h, bei deinem Tempo etwa Y h", und unter der Zeile einen Regelsatz je Fall (abgefangen / negativ / positiv / Montag / Urlaubswoche); Knopf heißt "So funktioniert die Liga"; `LigaUebersicht` hat Beispielwochen als Chips (Ziel −15, −5, neutral, +5, Ziel, +20 h). Die Hochrechnung zählt Tage vor der Installation als Arbeitstage mit 0 h (erste Woche dadurch zu niedrig, bewusst so gelassen). Skript 12 hat Claude am 9. September 2026 über Claude in Chrome im SQL-Editor ausgeführt und per pg_proc geprüft (neue Signatur, security definer).
- URLAUB (9. September 2026): Tabelle `urlaub` (von/bis als Kalendertage, Notiz; lesen alle, schreiben nur eigene) aus `supabase/09_urlaub.sql`, das auch `liga_stand()` neu anlegt (drop + create, neue Spalte `im_urlaub`). Urlaubstage senken den neutralen Punkt der Woche anteilig; seit 10. September 2026 über alle sieben Tage (das Team arbeitet Mo–So; `URLAUB_WOCHENTAGE = 7` in liga.ts, in der Datenbank ab Skript 13): neutral = (Gesamtziel − 10) × (7 − Urlaubstage)/7; ganze Urlaubswoche (7 Tage) = Erwartung 0, kostet nichts. Vorher Mo–Fr mit /5. Gleiche Formel in `src/shared/liga.ts` (`neutralStunden`, `urlaubstageInWoche`). `src/main/urlaub.ts` (Liste/Anlegen/Löschen ohne Cache), IPC `urlaub:eigene/anlegen/loeschen`, Einstellungen-Abschnitt "Urlaub" (Datumsfelder, Notiz, Liste mit Löschen), `LigaKarte` zeigt Urlaubstage und Erwartung der laufenden Woche, `LigaRangliste` markiert "im Urlaub". Skripte 9 und 10 hat der Auftraggeber am 9. September 2026 ausgeführt.
- LIGA-VORSCHAU als Hochrechnung (9. September 2026, weil "−120 am Mittwoch" entmutigte): `wochenPrognose` in `src/shared/liga.ts` überträgt den bisherigen Schnitt je Arbeitstag (Mo–Fr ohne Urlaub, heute anteilig zwischen 8 und 17 Uhr) auf die restlichen Arbeitstage und zeigt "Voraussichtlich +40, bei deinem Tempo etwa 46,0 h, ±0 ab 40,0 h". Montag vor Mittag (weniger als 0,4 Arbeitstage) steht "Prognose ab Montagmittag", am Wochenende der tatsächliche Stand "Diese Woche", bei ganzer Urlaubswoche "kostet nichts".
- Team-Screen: eigene Linie und eigener Balken grün (#00C076), andere blau/lila. Verlauf und Rangliste über 4 Wochen bis 12 Monate aus der Datenbankfunktion `team_wochen` (`supabase/06_team.sql`, am 9. September 2026 ausgeführt).
- Tagesschnitt für Hochrechnungen: nur Tage mit mindestens 1 Stunde produktiver Zeit zählen, heutiger Tag ausgenommen.
- ARBEITSTAGE (9. September 2026, Auftraggeber: "wir arbeiten auch am Wochenende, 50 h von Montag bis Sonntag"): Einstellung "Arbeitstage pro Woche" (5/6/7, Standard 7) in `src/renderer/src/arbeitstage.ts` (localStorage `arbeitstage`, Hook `useArbeitstage`, Chips unter Einstellungen → Wochenziele). Sie bestimmt den Tagesrichtwert (`tagesrichtwert(gesamtziel, arbeitstage)`), die Restlaufzeit (`verbleibendeArbeitstage(zeitpunkt, arbeitstage)`: Montag 7 … Sonntag 1) und die Hochrechnung (`wochenPrognose(..., arbeitstage)`, auch in der Team-Rangliste mit der Einstellung dieses Rechners). Der neutrale Punkt der Liga und der Urlaub bleiben bei Mo–Fr (Datenbank). WOCHENBALKEN (Rückfrage "zählt Unproduktives zur 10-h-Linie?"): nicht mehr gestapelt; der breite grüne Balken ist nur die produktive Zeit und allein sie gilt gegen die Richtwert-Linie (Beschriftung "7,1 h produktiv"), Unproduktives und Ungeklärtes stehen als schmale Balken daneben (barSize 7), Inaktives fehlt im Diagramm.
- Inaktivität beginnt rückwirkend beim letzten Tastendruck. Inaktiv-Block maximal 60 Minuten, danach ruht die Erfassung bis zur nächsten Eingabe.
- Blöcke unter 60 Sekunden gehen in den vorherigen Block, sonst in den nächsten. Pause erzeugt keinen Block.
- Neue Regeln wirken rückwirkend auf eigene, nicht von Hand geprüfte Blöcke. Von Hand geprüfte Blöcke fasst nie wieder eine Regel an.
- KURZWECHSEL (9. September 2026, "500 Aufgaben pro Tag, weil jeder App-Wechsel ein Block ist"): `alleNeuBewerten` in `src/main/bewertung.ts` rechnet erst alle Regel-Zielwerte, dann erben ungeklärte automatische Blöcke bis `KURZWECHSEL_SEKUNDEN` (5 min) zwischen zwei Nachbarn (Lücke ≤ 120 s) mit gleicher Bewertung produktiv/unproduktiv und gleichem Tätigkeitsschlüssel deren Tätigkeit; zwei Durchläufe, damit auch zwei kurze Blöcke hintereinander erben. Kein Flag am Block: die Übernahme wird bei jeder Neubewertung deterministisch neu hergeleitet und nur echte Änderungen werden geschrieben. Läuft zusätzlich bei jedem `erfassung`-Ereignis `bloecke`. Regeln schlagen die Übernahme, von Hand Geprüftes bleibt.
- EIGENES FENSTER ZÄHLT NICHT (10. September 2026, "die wessamedia-Zeit wertet man ja nicht als Arbeitszeit"): `verarbeiten()` in `src/main/erfassung.ts` erkennt das eigene Fenster an der Prozess-Kennung (`fenster.owner.processId === process.pid`, plattformunabhängig) und ersatzweise am Namen (`istEigenesProgramm` in `programme.ts`: "wessamedia Zeit" gebaut, "Electron" in der Entwicklung). Es entsteht nie ein eigener Block. Ein kurzer Blick bis `EIGENES_KURZ_MS` (2 min) lässt den vorherigen Block einfach weiterlaufen (wie Systemüberlagerungen), bleibt man länger, wird der vorherige Block rückwirkend beim Wechsel in die App (`eigenesSeit`) beendet und bis zum nächsten anderen Fenster nichts aufgezeichnet. `ErfassungsStatus.eigenesFenster` steuert den Text der Gerade-Karte ("Du bist gerade in wessamedia Zeit. Diese Zeit zählt nicht als Arbeit." bzw. "Kurzer Blick ... läuft noch bis zu 2 Minuten weiter"). Alte Blöcke der App selbst blendet `fehlbloeckeAusblenden(istFehlblock)` beim Start und nach jedem Abgleich aus (wie Systemfenster; beim ersten Start waren es 47).
- ABSCHNITTE ZUSAMMENGEFASST (10. September 2026, "zwei Blöcke vier Minuten, fünf Blöcke zehn Minuten, wieso nicht ein Block"): nur in der Anzeige, die Blöcke selbst bleiben getrennt gespeichert. `zeilenBilden` in `src/renderer/src/zeilen.ts` (seit 10. September 2026 eigenes Modul, auch für die Überschneidungsliste beim Eintragen: dort stehen die überschneidenden Blöcke als Zeilen unter dem Hinweis, "Zeile antippen" übernimmt Von und Bis in die Felder, `zeitenUebernehmen`; Wunsch: "sehen, was der Rechner in der Zeit gemacht hat, löschen und direkt das Telefonat eintragen") legt direkt aufeinanderfolgende automatische Blöcke (Lücke ≤ 2 min) mit gleichem Programm, bei Browsern gleicher Seite, gleicher Bewertung und gleicher Tätigkeit in eine Zeile (`Zeile` mit `bloecke`, Dauer = Summe der Abschnitte, Zeitspanne vom ersten bis zum letzten, Titel und Notiz vom längsten, Häkchen nur wenn alle geprüft); `BlockZeile` zeigt den Chip "N Abschnitte" (Props `abschnitte`, `sekunden`). Klick auf so eine Zeile öffnet den Gruppen-Dialog (`BlockDialog` mit `gruppe`, wie im Postfach), in der Mehrfachauswahl wählt sie alle Abschnitte. Der laufende Block und inaktive Blöcke bleiben für sich. Dauer-Spalte auf 80 px, damit "unter 1 min" nicht umbricht.
- FOKUS (10. September 2026, "ich lerne Instagram quer über Claude, Instagram und YouTube, und jeder Wechsel ist ein Block, den ich einzeln zuordnen muss"): Regeln kennen nur das Programm, nicht das Thema, deshalb setzt der Nutzer die Tätigkeit einmal selbst. `Fokus` (typen.ts: taetigkeit, seit) lebt in `Erfassung` (`fokusStarten(taetigkeit, beginn, jetzt)`, `fokusBeenden(jetzt)`, `fokusStand()`), nur im Speicher des Hintergrundprozesses, nicht in der Datenbank: jeder neue Arbeitsblock bekommt in `neuerBlock` über `fokusAnwenden` die Fokus-Tätigkeit, ist produktiv und `manuellGeprueft` (damit keine Regel und kein Kurzwechsel ihn später anfasst). Beginn darf rückwirkend liegen (heute, IPC `fokus:starten` begrenzt auf Tagesanfang bis jetzt): der laufende Block wird am Beginn geteilt (Rest unter 60 s: ganz übernommen), die gespeicherten Blöcke übernimmt `fokusRueckwirkend` in `src/main/fokus.ts` (automatische Arbeitsblöcke im Zeitraum, auch schon bewertete; ein Block, der vor dem Beginn anfängt, wird am Beginn geteilt; inaktive Blöcke, Hand-Einträge und der laufende Block bleiben). Beenden schließt den laufenden Block, der nächste wird wieder nach Regeln bewertet. Endet von selbst um Mitternacht (`verarbeiten`) und beim Wechsel in "abwesend" (60 min ohne Eingabe), bei Abmeldung sowieso. `ErfassungsStatus.fokus`; Kopfzeile zeigt links den grünen Chip "Fokus: Instagram Learning · seit 09:01" und rechts "Fokus beenden" statt "Fokus"; Heute-Karte "Gerade" mit grünem Knopf "Fokus starten" (knopf-primaer) bzw. Hinweiszeile und "Fokus beenden"; Tray-Menü zeigt den Fokus und "Fokus beenden". Dialog `FokusDialog.tsx` (CustomEvent `fokus-dialog`, `fokusDialogOeffnen()`, Halter in App.tsx, Strg/Cmd+F): Tätigkeits-Chips mit Symbol plus Freitext, Beginn "Jetzt / Vor 15 Minuten / Vor 30 Minuten / Vor 1 Stunde / Seit Uhrzeit (HH:MM in Berliner Zeit über berlinZuUtc)". Dazu in der Mehrfachauswahl "Alles dazwischen" (ab 2 ausgewählten: alle Arbeitsblöcke zwischen dem frühesten und dem spätesten dazunehmen, `alleDazwischen` in HeuteScreen), um eine ganze Phase nachträglich auf einmal zuzuordnen; `zeileKlick` setzt die Auswahl funktional, sonst ging bei zwei schnellen Klicks einer verloren. Eigenheit: Ein Fokus-Block unter 60 s beim Beenden wandert nach der Kurzblock-Regel in den Nachbarblock.
- POSTFACH nach Programm gebündelt (9. September 2026): `UngeklaertPostfach` lädt die Liste sofort, gruppiert nach Programm (sortiert nach Dauer), Knopf "Alle N zuordnen" öffnet `BlockDialog` mit `gruppe`: oben die Vorgabe (Tätigkeit, Bewertung), darunter jeder Block einzeln mit Datum, Zeit, Dauer und vollem Fenstertitel sowie eigener Bewertung ("wie oben" / produktiv / unproduktiv) und eigener Tätigkeit (`einzeln`-State); beim Speichern werden gleiche Ergebnisse gebündelt per `bloecke:mehrereAendern` geschrieben; keine Zeitfelder, Regel nur nach Programm ("alle künftigen Blöcke von X wie die Vorgabe"), Löschen für alle. "Einzeln durchgehen" bleibt. FENSTERTITEL lesbar (9. September 2026, "welches YouTube-Video, welcher Tab"): `src/shared/fenster.ts` zerlegt Browser-Titel in Seite (YouTube, Google Sheets, Gmail, Instagram, Asana, Claude ... über `SEITEN`, sonst letztes Titelsegment ≤ 30 Zeichen) und Tab-Titel (Browser-Anhang, Profil-Anhang und "(3) "-Zähler entfernt); `BlockZeile` zeigt die Seite als Chip und den bereinigten Tab-Titel, das Postfach gruppiert Browser-Blöcke zusätzlich nach Seite ("Google Chrome · YouTube"), der Gruppen-Dialog bietet dann eine Titel-Regel mit der Seite als Muster an (`gruppeMuster`). Erfassung sieht nur das fokussierte Fenster (get-windows `activeWindow`), zweiter Bildschirm ohne Fokus zählt nicht mit; dem Auftraggeber am 9. September 2026 erklärt. SEITE VORNE (10. September 2026, "da stand immer noch Chrome und nicht YouTube"): `anzeigeName(programm, fenstertitel)` in `fenster.ts` liefert `haupt` (im Browser die Seite, sonst das Programm), `neben` (im Browser der Browser, sonst null) und `titel`; damit zeigen `BlockZeile`, die Gerade-Karte (vorher rohes Programm und roher Fenstertitel), das Postfach und der `BlockDialog`-Titel "YouTube · Google Chrome" statt "Google Chrome · YouTube". `SEITEN` auf rund 100 Einträge erweitert (Google-Familie, Meta Business Suite, Meta Ads, Trello, Slack, Teams, Canva, CapCut, Figma, Frame.io, Dropbox, WeTransfer, KI-Werkzeuge, Shop- und Buchhaltungssysteme, Stock-Seiten ...), speziellere Muster vor allgemeinen; unbekannte Seiten fallen weiter auf das letzte Titelsegment ≤ 30 Zeichen zurück. Auf dem Mac gibt es keine Fenstertitel, dort bleibt der Browsername vorne.
- TÄTIGKEIT ALS CHIPS im Block-Dialog (11. September 2026, "da steht KI Learning, ich kann es gar nicht zu YouTube Learning ändern"): Das Feld war ein Texteingabefeld mit `<datalist>`, die anderen Tätigkeiten erschienen erst beim Tippen, das fiel dem Auftraggeber nicht auf. `BlockDialog` zeigt jetzt wie der Fokus-Dialog alle bekannten Tätigkeiten als Chips mit Symbol (Antippen setzt sie, aktive Chip dunkel auf hell; Vergleich über `schluessel` ohne Groß/Klein, Leerzeichen, Bindestriche), darunter das Feld "oder eine neue Tätigkeit eintippen" (Datalist bleibt); Tätigkeit über beide Spalten, Bewertung darunter. Dem Auftraggeber erklärt: Zeile anklicken → Chip antippen → Speichern; Block ist danach von Hand geprüft; laufender Block erst nach Abschluss; für wechselnde Tätigkeiten im selben Programm den Fokus nutzen statt einer Regel. NEUE TÄTIGKEIT SICHTBAR (gleicher Tag, "es braucht ein Feld, wo man selbst eine Tätigkeit reinschreibt, die dann künftig zur Auswahl steht, und einheitlich geschrieben"): Das Feld gab es (Datalist unter den Chips), zeigte aber den aktuellen Namen und wirkte nicht wie "neu anlegen". Jetzt ein gestrichelter Chip "+ Neue Tätigkeit" (`Plus`), der ein LEERES Feld öffnet (`neuOffen`; das Feld ist auch sichtbar, wenn der Name zu keinem Chip passt oder es noch keine Tätigkeiten gibt). Beim Tippen prüft `namenPruefen(eingabe, bekannt)` live: gleicher Schlüssel wie ein vorhandener Name → "Gibt es schon, wird als X gespeichert" (die Vereinheitlichung macht ohnehin `Taetigkeiten.merken` im Hauptprozess: Schlüssel ohne Groß/Klein, Leerzeichen, Bindestriche, vorhandene Schreibweise gewinnt, neue Namen gehen in die Tabelle `taetigkeit` und stehen allen zur Verfügung); Levenshtein-Abstand ≤ 2 oder Präfix (ab 4 Zeichen) → "Meintest du X?" mit Knopf zum Übernehmen (Tippfehler wie "Manageing"); sonst "Neue Tätigkeit: steht ab dem Speichern für alle als Chip zur Auswahl. Symbol unter Einstellungen → Tätigkeiten und Symbole". Die Liste im Fenster (`useTaetigkeiten`, IPC `taetigkeiten:liste`) lädt bei jeder Blockänderung neu, darum erscheint der neue Chip sofort nach dem Speichern.
- RÜCKFRAGE NACH ABWESENHEIT und "ICH BIN WEG" (11. September 2026, Go des Auftraggebers für die Liste "Nicht zu Ende gedacht": "Abwesenheit ist immer rot" und "ein Klick zu viel bei Terminen"). (1) Rückfrage: `Erfassung.offeneAbwesenheiten()` liefert abgeschlossene Blöcke ohne Programm (rot "Nicht am Rechner" oder blau "Abwesend") aus den letzten `RUECKFRAGE_FENSTER_MS` (24 h) mit Dauer von der Untätigkeits-Schwelle bis `RUECKFRAGE_MAX_MS` (3 h), nicht von Hand geprüft, nicht der laufende, nicht "später" weggeklickt, keine an Mitternacht geteilten Nachtstücke (Beginn oder Ende genau 0 Uhr), doppelte Blöcke über denselben Zeitraum nur einmal (die Antwort gilt per `abwesenheitBloecke` in index.ts für alle Doppelgänger); steht im Status (`ErfassungsStatus.offeneAbwesenheiten`, Typ `Abwesenheit`). `AbwesenheitKarte` (Heute, zwischen Gerade-Karte und Postfach, nur am heutigen Tag) zeigt je Abwesenheit "Du warst 42 Minuten weg (10:05 bis 10:47)" mit Knöpfen: Pause (IPC `abwesenheit:pause`, Block soft-gelöscht, zählt nirgends), alle Tätigkeiten als Chips plus "+ Neue Tätigkeit" (`abwesenheit:zuordnen`: `eintragAnlegen` als produktiver Hand-Block über denselben Zeitraum und der rote Block wird gelöscht), Privat (`abwesenheit:privat`: `blockAendern` mit leerer Änderung, bleibt rot, gilt als geprüft), Später (`abwesenheit:spaeter`: Kennung 2 Tage in der Ablage gemerkt). Beim Schließen des Ruheblocks nach der Rückkehr (Zweig "Wieder Eingaben") und nach dem Füllen von Lücken (Aufwachen, Takt-Lücke; nicht beim App-Start) meldet `abwesenheitMelden` das Ereignis `abwesenheit`; `index.ts` zeigt dann eine Systemmeldung "Du warst N Minuten weg", wenn das Fenster nicht sichtbar und fokussiert ist (Klick öffnet es). Nach 9 Stunden Schlaf wird nicht gefragt (über 3 h). (2) "Ich bin weg": Kopfzeile-Knopf (DoorOpen) öffnet `WegDialog` (wie der Fokus-Dialog: Chips, neues Feld, Beginn Jetzt/15/30/60 min/Uhrzeit, höchstens 3 h zurück); IPC `weg:starten` → `Erfassung.wegStarten(taetigkeit, beginn, jetzt)`: Fokus endet, laufende Blöcke enden am Beginn, automatische Blöcke im Zeitraum werden gekappt oder soft-gelöscht (schon rot gebuchte Zeit gehört zum Termin), ein Hand-Block (quelle manuell, produktiv, von Hand geprüft, Programm null) läuft als `wegBlock`, Zustand `weg` (neuer Wert in `ErfassungsZustand`), `ErfassungsStatus.weg` (Typ `Weg`). `wegVerarbeiten` verlängert den Block je Takt und erkennt die Rückkehr: erst wenn seit dem Start einmal `WEG_ABWESEND_S` (60 s) keine Eingabe kam, gilt die nächste Eingabe (`getSystemIdleTime` < `WEG_ZURUECK_S` 10 s) als Rückkehr, der Block endet rückwirkend zum Eingabezeitpunkt; sonst spätestens nach `WEG_MAX_MS` (12 h) oder um Mitternacht. Unter 60 s wird kein Eintrag gespeichert. Lücken (Takt, Ruhezustand, Sperre) werden während "weg" nicht rot gefüllt, der Hand-Block deckt sie; ein Neustart übernimmt eine laufende Abwesenheit (Ablage `abwesenheit-<uid>.json` mit `weg`+`blockId` und `spaeter`, bis 12 h alt), `start()` füllt dann auch keine Lücke. Kopfzeile: Text "Weg: Kundentermin seit 10:05, zählt als produktiv", Knopf "Zurück" (`weg:beenden`), Fokus und Pause sind währenddessen gesperrt; Gerade-Karte erklärt es; Tray zeigt "Weg: …" und "Zurück (Abwesenheit beenden)". Für eine Pause, die nicht zählen soll, bleibt der Pause-Knopf. Beim ersten Test hat ein per Fernsteuerung ausgelöster Klick auf "Telefonat" nicht den Chip im Dialog, sondern den gleichnamigen Chip in der Rückfrage-Karte getroffen und eine echte Nachtabwesenheit (00:00–02:49) als Telefonat gebucht; rückgängig gemacht über `bloecke:aendern` (Löschen des Hand-Blocks) und direktes Zurücksetzen von `geloeschtAm` im lokalen Speicher `bloecke-<uid>.json` bei gestoppter App (Kennung in `ausstehend` eingetragen, damit die Datenbank die Wiederherstellung übernimmt; lokal Ausstehendes gewinnt beim Abgleich). Lehre: Test-Klicks immer auf den Dialog eingrenzen (`closest('.glas')`). Außerdem zählt `wegVerarbeiten` nur Ruhe SEIT dem Start (Minimum aus Leerlaufzeit und Zeit seit Start), sonst galt die nächste Mausbewegung sofort als Rückkehr, wenn man vor dem Klick schon länger untätig war.
- DIAGNOSE KOPIEREN (11. September 2026, "niemand sieht, wenn es bei den Kollegen hakt"): `src/main/protokoll.ts` hängt sich beim Start (`protokollEinrichten()` in index.ts) an console.error/console.warn sowie uncaughtException/unhandledRejection und merkt die letzten 80 Zeilen im Speicher (keine Datei). IPC `system:diagnose` baut einen Text: Version und ob installiert, System (Plattform, Release, Electron, Node), Zeit in Berlin, Autostart, Bildschirmaufnahme, Datenbank konfiguriert, Konto (8 Zeichen), Erfassungszustand mit laufendem Block/Fokus/Weg, Untätigkeits-Schwelle, Fenstertitel-Schalter, Blockzahlen (lokal, heute, ungeklärt, offene Rückfragen), Abgleich (letzter, wartend, Fehler), Anzahl Regeln und Tätigkeiten, Protokollzeilen. Keine Blockinhalte, keine Fenstertitel. Einstellungen → System → "Diagnose kopieren" legt den Text per `navigator.clipboard.writeText` in die Zwischenablage ("einfach in den Chat einfügen").
- WORTMARKE GEDIMMT außerhalb von "Heute" (11. September 2026): `App.tsx` gibt `<Hintergrund gedimmt={aktiv !== 'heute'} />`, `HintergrundLogo` setzt die vordere Wortmarken-Ebene auf Deckkraft 0,4 mit 500 ms Übergang, weil auf Woche, Auswertung und Team Diagramme und Tabellen mitten durch die Schrift laufen; auf Heute liegen die Karten unter der Schrift (gemessener Abstand) und sie bleibt voll.
- KUNDE (PROJEKT) ALS ZWEITE DIMENSION (11. September 2026, Go des Auftraggebers: "für wen" ist in einer Agentur oft wichtiger als "was"): `Block.kunde: string | null` (Spalte `kunde` auf `block`), gemeinsame Kundenliste in der Tabelle `kunde` (Skript `supabase/14_kunde.sql`, gleiche Regeln wie `taetigkeit`: lesen/anlegen/ändern für alle Angemeldeten, `schluessel` eindeutig). `src/main/kunden.ts` (`Kunden`: `merken` vereinheitlicht Schreibweisen über `taetigkeitSchluessel`, `liste`, `ausBloecken`, `laden`, `hochladen` mit ignoreDuplicates), IPC `kunden:liste`, Renderer-Hook `useKunden` (`kunden.tsx`), Baustein `KundenWahl` (Chips "Kein Kunde", alle Kunden mit Gebäude-Symbol, "+ Neuer Kunde" mit Feld; `kompakt` im Block-Dialog). Kunde setzbar im Block-Dialog (einzeln: `BlockAenderung.kunde`, leer = kein Kunde; Gruppe: nur wenn gewählt), im Fokus (`Fokus.kunde`, `fokusAnwenden` und `fokusRueckwirkend` setzen ihn), bei "Ich bin weg" (`Weg.kunde`, Hand-Block) und beim Eintragen (`NeuerEintrag.kunde`); `eintragAnlegen`/`blockAendern` bekommen `kunden` als Parameter. `BlockZeile` zeigt den Kunden als Chip. Auswertung: Karte "Verteilung nach Kunden" (`verteilungNachKunde` in auswertung.ts, "Ohne Kunde" grau) aus den lokalen Blöcken, nur wenn Kunden vergeben sind, Hinweis bei Zeiträumen über 13 Wochen. Regeln kennen keinen Kunden (bewusst). Mehrfachauswahl und Postfach-Gruppen setzen keinen Kunden (Mehrfachleiste hat nur Tätigkeit/Bewertung). SCHUTZ OHNE SKRIPT: `sync.ts` prüft im Anfangsabgleich mit `select kunde ... limit 1`, ob die Spalte existiert (`spalteKunde`); fehlt sie, lässt `zuZeile` das Feld weg (sonst scheiterte jeder Abgleich mit "column kunde does not exist"), Kunden bleiben dann lokal, `Kunden.laden/hochladen` fangen Fehler ab, `Sync.kundeSpalteVorhanden()`. Skript 14 liegt auch als `Skript-14-fuer-Supabase.sql` im Dokumente-Ordner; ausführen muss es der Auftraggeber (oder Claude über Claude in Chrome).
- KUNDENLISTE UND KUNDENVERWALTUNG (11. September 2026, Rückfrage des Auftraggebers "wo sehe ich, wie viel Zeit in welche Kunden geflossen ist"; Go für Karte plus Verwaltung): Auswertung-Karte "Kunden, letzte …" (`KundenTabelle.tsx`, ersetzt die Überschrift "Verteilung nach Kunden"; erscheint, sobald ein Block einen Kunden hat ODER die Teamliste Kunden kennt): je Kunde produktive Stunden im Zeitraum, Balken (Maßstab = größte Zeile inklusive "Ohne Kunde"), Anteil an der produktiven Zeit, letzter Einsatz; sortiert nach Stunden; darunter die Zeile "Ohne Kunde" grau, dann der bisherige Verteilungsring, dann "Ohne Zeit in diesem Zeitraum: …" für Kunden der Teamliste ohne Blöcke. Rechnet aus den lokalen Blöcken (13 Wochen), Blöcke werden am Zeitraum-Rand anteilig gezählt, Zusammenführung über `taetigkeitSchluessel`. Einstellungen-Karte "Kunden" (`KundenVerwaltung.tsx`, vor "System"): Liste des Teams mit "Umbenennen" (Feld inline, Speichern per Enter oder Knopf) und Papierkorb mit Rückfrage "Aus allen Blöcken entfernen? Ja, löschen / Nein". Beides wirkt auf das GANZE TEAM über die Datenbankfunktionen aus `supabase/15_kunde_verwalten.sql` (security definer, nur für Angemeldete): `kunde_umbenennen(alt_schluessel, neuer_name)` schreibt den neuen Namen in alle nicht gelöschten Blöcke aller Personen mit passendem Schlüssel (`geaendert_am = now()`), legt bei gleichem Schlüssel eines vorhandenen Kunden beide zusammen (Zeile in `kunde` löschen + Upsert), liefert die Zahl geänderter Blöcke; `kunde_loeschen(alt_schluessel)` setzt `kunde = null` und entfernt die Zeile. `Kunden.umbenennen/loeschen` (kunden.ts, mit `speicher`) rufen das RPC, ziehen danach die EIGENEN lokalen Blöcke sofort nach (`speicher.aktualisieren`, damit sie als ausstehend hochgehen und der Abgleich sie nicht mit der alten Fassung überschreibt) und pflegen die Liste; die Kollegen bekommen die neuen Namen beim nächsten Abgleich (geaendert_am ist neuer). IPC `kunden:umbenennen(alt, neu)` / `kunden:loeschen(name)` lösen danach `bloeckeGeaendert()` und `statusVerteilen()` aus. Fehlt das Skript, meldet PostgREST "Could not find the function …", die App zeigt "Dafür muss in Supabase einmal das Skript 15 (15_kunde_verwalten.sql) ausgeführt werden" als Hinweis (geprüft in der Entwicklungsversion vor dem Ausführen). Die Tabelle `kunde` hat bewusst KEINE Delete-Policy, Löschen geht nur über die Funktion. Beim Test am 11. September 2026 wurde ein Hand-Block mit Kunde "Testkunde" angelegt und wieder gelöscht; der Name "Testkunde" blieb bis zum Skript in der Tabelle `kunde`. SKRIPT 15 AUSGEFÜHRT am 11. September 2026 von Claude über Claude in Chrome (SQL-Editor, `setValue` per monaco, "Run", Dialog "Potential issue detected" → "Run query", "Success. No rows returned"; Prüfung per pg_proc: beide Funktionen vorhanden, security definer); danach `kunden:loeschen("Testkunde")` aus der installierten App per CDP: 0 Blöcke, Liste leer. Die Chrome-Erweiterung war erst erreichbar, nachdem der Auftraggeber sie im Profil "Geschäftlich" mit info@wessamedia.com angemeldet hatte (dem Konto dieser App); ein erster Versuch mit moritz@wessamedia.com hätte ein zweites Claude-Konto angelegt. Das Electron-Fenster und Chrome liegen auf dem zweiten Monitor (computer-use: `switch_display`), Chrome ist für computer-use nur lesbar (Stufe "read"). Test-Klicks per CDP müssen auf den sichtbaren Screen begrenzt werden (`.screen-ebene:not(.screen-versteckt)`): alle Screens bleiben im DOM, `.glas`-Suche nach "Kunden" traf zuerst die Auswertung-Karte; die Überschriften sind per CSS in Großbuchstaben, `innerText` liefert sie so.
- LEERE BLÖCKE NIE "WARTEND" (11. September 2026): Der Abgleich sendet nur Blöcke mit Dauer, Blöcke mit Ende = Anfang blieben deshalb für immer in `ausstehend` (Einstellungen zeigten dauerhaft "2 warten", Diagnose "wartend 2"; ein Claude-Block vom 9. September und ein gelöschter Leerblock). `Sync` nimmt sie jetzt zu Beginn jedes Durchlaufs per `alsGesendet` aus der Warteliste; ändert sich so ein Block noch (der laufende Block ist im ersten Takt leer), trägt `aktualisieren()` ihn wieder ein.
- AUTOSTART-ANZEIGE (11. September 2026): Die Diagnose des Auftraggebers zeigte "Autostart: false" und der Schalter unter Einstellungen → Erfassung stand auf aus, obwohl der Registry-Eintrag `HKCU\...\Run\com.wessamedia.zeit` = `"…\wessamedia Zeit.exe" --hidden` vorhanden war. Ursache: `app.setLoginItemSettings({ openAtLogin, args: ['--hidden'] })` schreibt den Eintrag MIT Argument, `app.getLoginItemSettings()` OHNE `args` vergleicht unter Windows gegen einen Eintrag ohne Argumente und meldet false. `autostartAn()` in index.ts fragt jetzt mit `{ args: ['--hidden'] }` ab (unter macOS wird die Option ignoriert); Einstellungen, Diagnose und die Antwort von `system:autostartSetzen` nutzen sie. Der Autostart war beim Auftraggeber die ganze Zeit an.
- NUR IM FOKUS AUFZEICHNEN (12. September 2026, Entscheidung des Auftraggebers: "dass unser Bildschirm durchgehend aufgenommen wird, machen wir raus; alles einzeln zuordnen ist viel zu anstrengend; nur noch über den Fokusmodus, die Tabs werden weiterhin getrackt"): `Erfassung.nurFokus` (Standard true, in der Ablage `abwesenheit-<uid>.json` gemerkt, Schalter "Nur im Fokus aufzeichnen" unter Einstellungen → Erfassung, IPC `erfassung:nurFokusSetzen`, `ErfassungsStatus.nurFokus`). Ohne Fokus und ohne "Ich bin weg" (`ohneFokus()`) entsteht KEIN Block: neuer Zustand `ohne-fokus` (`ErfassungsZustand`), `verarbeiten` ruft nur `ohneFokusVerarbeiten` (offene Blöcke schließen, keine Untätigkeit, keine Lücken beim Start/Aufwachen/Takt), Kopfzeile "Kein Fokus, Zeit zählt nicht" mit grünem Hauptknopf "Fokus", Gerade-Karte erklärt es, Tray zeigt "Kein Fokus, Zeit zählt nicht" und "Fokus starten …" (`TrayAktionen.fokusStarten` → `fokusDialogOeffnen` in index.ts: Fenster zeigen + IPC-Ereignis `fokus:dialogOeffnen`, Renderer `api.fokus.onDialogOeffnen` in App.tsx). ERINNERUNG: nach 10 Minuten aktiver Zeit am Rechner ohne Fokus (`OHNE_FOKUS_ERINNERUNG_MS`, Leerlauf unter 60 s zählt) einmal das Ereignis `ohne-fokus` → Systemmeldung "Kein Fokus aktiv … Klicken und Fokus starten", höchstens alle 45 Minuten (`OHNE_FOKUS_ABSTAND_MS`), nicht wenn das Fenster sichtbar und fokussiert ist; Untätigkeit ab der Schwelle setzt den Zähler zurück. IM FOKUS läuft alles wie bisher (Programme, Fenstertitel, Untätigkeit rot, Abwesend blau beendet den Fokus, Mitternacht beendet ihn), nur eben produktiv mit der Fokus-Tätigkeit. RÜCKWIRKENDER START ohne Aufzeichnung davor: `fokusNachtragen(beginn, jetzt)` (nach `fokusRueckwirkend` in `fokus:starten`) legt für jede Lücke ab 60 s zwischen Beginn und jetzt, die kein Block deckt, einen produktiven Hand-Block (quelle manuell, Programm null) mit der Fokus-Tätigkeit an; `sofort()` startet danach gleich den ersten Takt. FOKUS ÜBERLEBT NEUSTART: der laufende Fokus steht in der Ablage (`fokus`), beim Start wird er übernommen, wenn er am selben Tag und unter 12 h (`FOKUS_MAX_MS`) liegt (sonst ginge nach einem Update still Zeit verloren); `stop()` löscht ihn nicht aus der Ablage. Lange Lücke ab 90 min (`lueckeFuellen` blau) beendet den Fokus wie die Abwesenheit. Schalter aus = altes Verhalten (durchgehend, Regeln, Postfach); Regeln-Karte sagt dann, dass sie nur noch für alte Blöcke gelten. Geprüft in der Entwicklungsversion: ohne Fokus 16 s lang kein Block, Fokus → Block "Claude · Telefonat · produktiv · geprüft", Ende → ohne-fokus, rückwirkend 20 min → Hand-Blöcke für die Lücken, Schalter aus → laeuft mit Regel-Block, an → ohne-fokus. TEST-LEHRE (12. September 2026): Beim Aufräumen nach dem Test wurden Blöcke über ein ZEITFENSTER gelöscht statt über die gemerkten Kennungen; das traf den echten Nachtblock (22:34–06:38, blau) und zwei echte Claude-Blöcke von 06:39–06:46, die der rückwirkende Test-Fokus vorher auf "Telefonat" umgestellt hatte (`fokusRueckwirkend` fasst echte Auto-Blöcke an!). Wiederhergestellt per Scratchpad `wiederherstellen-bloecke.cjs` bei gestoppter App (geloeschtAm null, manuellGeprueft false, in `ausstehend`), danach hochgeladen; die Fenstertitel waren durch das Löschen weg, deshalb griff die Regel nicht mehr (Blöcke wurden ungeklärt) und die beiden bekamen per `bloecke:aendern` "KI Learning"/produktiv wie zuvor. Regel: Testblöcke nur über ihre Kennungen löschen, rückwirkende Fokus-Tests nur in Zeiträumen ohne echte Blöcke.
- TÄTIGKEITEN AM RECHNER ODER UNTERWEGS (12. September 2026, Auftraggeber: "Dreh, Fahrt, Kundertermin machen im Fokusmodus keinen Sinn, aus dem Fokus rausnehmen"; Hintergrund: ein Fokus "Dreh" bei zugeklapptem Laptop würde nach 3 min rot und nach 90 min blau, dafür ist "Ich bin weg" da): Spalte `unterwegs boolean default false` auf `taetigkeit` (Skript `supabase/16_taetigkeit_unterwegs.sql`, setzt dreh/fahrt/kundentermin/termin/telefonat/kundenbesuch/vorort/aussentermin gleich auf true; von Claude am 12. September 2026 über Claude in Chrome ausgeführt, Ergebnis: Dreh, Fahrt, Kundentermin, Telefonat unterwegs, 10 weitere am Rechner). `Taetigkeiten` (main) führt `unterwegs` je Eintrag, `laden()` fragt die Spalte mit ab und fällt bei Fehler auf die alte Auswahl zurück (`spalteUnterwegs`, Warnung "Spalte unterwegs fehlt noch (Skript 16)" im Protokoll), `unterwegs()` liefert die Schlüssel mit true, `unterwegsSetzen(name, an)` upsert auf `taetigkeit` (ohne Spalte Fehler "Skript 16"), `kennt(name)`. IPC `taetigkeiten:unterwegs` / `taetigkeiten:unterwegsSetzen` (löst `bloecke:aenderung` aus, daran hängen die Listen im Fenster). Renderer `useUnterwegs()` und `useTaetigkeitenNachOrt()` (`taetigkeiten.tsx`): `amRechner` für den `FokusDialog` (mit Hinweis "Dreh, Fahrt, Kundentermin … unter Ich bin weg"), `unterwegs` für `WegDialog` und `AbwesenheitKarte`; solange keine Tätigkeit eingeordnet ist (`eingeordnet` false), bekommen alle die ganze Liste. Eine im Weg-Dialog oder in der Rückfrage NEU angelegte Tätigkeit wird automatisch unterwegs (`weg:starten`, `abwesenheit:zuordnen`: vorher `kennt()` prüfen). Einstellungen → Tätigkeiten und Symbole: je Zeile rechts ein Chip "Am Rechner" (Monitor) / "Unterwegs" (DoorOpen, grün), Antippen wechselt für das ganze Team, Hinweis unten. Block-Dialog, Eintragen, Heute und Woche nutzen weiter die volle Liste (`useTaetigkeiten`). Geprüft in der Entwicklungsversion: Fokus-Chips ohne die vier, Weg-Chips nur die vier, Orga hin- und zurückgeschaltet (Karte, Hinweis, IPC-Karte).
- Hand-Einträge warnen bei Überschneidung mit automatischen Blöcken. Sie starten als produktiv, bleiben aber umstellbar.
- Löschen blendet nur aus (geloescht_am) und leert sofort Fenstertitel und Notiz.
- Tätigkeitsnamen: Vergleich ohne Groß/Klein, Leerzeichen und Bindestriche; Symbole je Tätigkeit gelten für das ganze Team.
- lucide-Symbole immer in Weiß. Orange (#FE5303) nur für Rang-Ring, Abzeichen, Auszeichnungen und die Rang-Aufstieg-Einblendung.
- Supabase-URL und Publishable key werden beim Bauen fest in die App eingebaut; die `.env` bleibt nur lokal. Secret key wird nie gebraucht (Testdaten als SQL-Skript im Supabase-Editor). Selbstregistrierung in Supabase abschalten. Region Frankfurt.
- Unsignierte Installer. Mac-Installer entstehen über GitHub Actions (Windows kann keine .dmg bauen); Zustimmung des Auftraggebers am 9. September 2026 ("automatisch über GitHub bauen"). GITHUB (9. September 2026): Konto `Moritzrx` (vom Auftraggeber angelegt), Repository `Moritzrx/zeiterfassung`, PRIVAT, von Claude per GitHub CLI angelegt (`gh repo create --private --source=. --push`). GitHub CLI 2.100 per winget installiert (`C:Program FilesGitHub CLI`, in der Bash `export PATH="/c/Program Files/GitHub CLI:$PATH"`); Anmeldung über den Gerätecode (`gh auth login --web`, Code hat der Auftraggeber selbst auf github.com/login/device bestätigt; Claude gibt niemals Zugangsdaten ein). `gh auth setup-git` macht gh zum Credential-Helper, sonst öffnet `git push` den Git Credential Manager mit einem Anmeldefenster und hängt. Die Claude-Chrome-Erweiterung war dafür nicht erreichbar (in den Profilen "Mein Chrome" und "wessamedia.com" installiert, aber nicht verbunden). Repository-Secrets `VITE_SUPABASE_URL` und `VITE_SUPABASE_PUBLISHABLE_KEY` per `gh secret set` aus der lokalen .env gesetzt (Publishable key darf eingebaut werden). Ablauf starten: `gh workflow run installer.yml`, verfolgen: `gh run view <id> --json status,conclusion,jobs`, Fehler: `gh run view <id> --log-failed`. Erster Lauf scheiterte in beiden Jobs, weil electron-builder auf CI mit `publish`-Konfiguration selbst veröffentlichen will und ein GH_TOKEN verlangt; seitdem `--publish never` an allen drei electron-builder-Aufrufen im Ablauf. Seit 9. September 2026 ÖFFENTLICH (Entscheidung des Auftraggebers: "öffentlich"), damit die App die Release-Dateien ohne Anmeldung laden kann; vorher die ganze Git-Geschichte auf Schlüssel geprüft (Publishable key, sb_secret, service_role, .env: nie enthalten). Der Auto-Modus-Klassifizierer blockiert `gh repo edit --visibility public` in Kombination mit anderen Befehlen, allein lief es. NEUE VERSION veröffentlichen: `npm version 1.0.2 --no-git-tag-version`, committen, `git tag -a v1.0.2 -m "Version 1.0.2"`, `git push origin main --tags`; der Ablauf hängt exe, blockmap, latest.yml und dmg an die Veröffentlichung, die installierten Windows-Apps holen sie sich beim nächsten Prüflauf. Erste Veröffentlichung: v1.0.1 am 9. September 2026; v1.0.2 direkt danach, weil `quitAndInstall(false, true)` in 1.0.1 noch den NSIS-Assistenten gezeigt hätte, seit 1.0.2 `quitAndInstall(true, true)` (still, /S, danach Neustart). Geprüft am 9. September 2026: Die installierte 1.0.0 hat 1.0.1 im Hintergrund geladen (Zustand bereit) und beim sauberen Beenden still installiert (`autoInstallOnAppQuit`); sauber beenden ohne Klick geht per CDP `Browser.close` auf dem Browser-Ziel (Scratchpad `beenden.cjs`), Stop-Process würde die Installation überspringen. Die Release-Dateien liegen unter github.com/Moritzrx/zeiterfassung/releases/latest; `latest.yml` ist die Datei, die electron-updater liest.
- Content-Security-Policy nur in der gebauten App als Meta-Tag (siehe Schritt 12). In der Entwicklung bewusst ohne CSP, damit Hot Reload läuft.
- AUTOMATISCHE UPDATES (9. September 2026, Auftrag "alles Nötige übernehmen"): `src/main/aktualisierung.ts` mit electron-updater 6 (Laufzeit-Abhängigkeit). Windows: lädt den neuen Installer im Hintergrund aus der GitHub-Veröffentlichung (braucht dort latest.yml, .exe und .blockmap; der Ablauf hängt sie beim Tag v* an) und spielt ihn beim Neustart ein (`quitAndInstall`). Mac: ohne Apple-Signatur kein Selbst-Update, deshalb nur Prüfung über die GitHub-API `releases/latest` und "Download öffnen" (Release-Seite). Prüfung 30 s nach dem Start und alle 4 h, dazu "Jetzt prüfen" in Einstellungen → System → Updates; Leiste `UpdateHinweis` über der Navigation, wenn eine Version bereitliegt. `publish: github` in electron-builder.yml (owner/repo müssen zum echten Repository passen; `REPO_BESITZER`/`REPO_NAME` in aktualisierung.ts ebenso). Ohne GH_TOKEN veröffentlicht electron-builder lokal nichts, schreibt aber app-update.yml und latest.yml. Typ `UpdateStatus` in typen.ts, IPC `update:status/pruefen/installieren`, Ereignis `update:status`. Neue Version = package.json-Version erhöhen, committen, Tag `vX.Y.Z` pushen.
- FEHLERSCHUTZ (9. September 2026): `Fehlerschutz.tsx` (Klassen-Komponente mit componentDidCatch) um jeden Screen-Inhalt und die Aufstiegs-Einblendung in `App.tsx`: bei einem Fehler zeigt nur der Bereich eine Karte mit Meldung, "Neu laden" (baut den Bereich per Schlüssel neu) und "App neu starten" (location.reload); Erfassung und Rest laufen weiter. Vorher wurde bei einem Renderer-Fehler die ganze App schwarz.
- MEDAILLEN als Bilder (9. September 2026, 14 × 2 = 28 Higgsfield-Credits, Modell nano_banana_pro mit dem Champion-Wappen als `image_references`, Rolle heißt bei diesem Modell so, nicht `reference`): je Auszeichnungstyp ein rundes Goldmedaillon mit farbigem Band und Motiv in der Auszeichnungsfarbe (Pokal, drei Ringe, Flamme, Schwerter, Eis-Unendlich, Lorbeer mit fünf Sternen, Zielscheibe, roter Edelstein, Berg mit Fahne, geflügelter Schuh, Sonnenaufgang, Eule mit Mond, Helm mit Schwert und Schild, Haken mit Besen), gleiche Umwandlung wie die Wappen (`wappen-verkleinern.cjs`, Namen `ausz-<typ>`), Dateien `src/renderer/src/assets/wappen/ausz-*.png` (130–235 kB). `AuszeichnungBild.tsx` zeigt nur noch das Bild (10 % über den Rahmen), erreichte mit drop-shadow in der Auszeichnungsfarbe, offene grau (grayscale, brightness 0,5, Deckkraft 0,5); die SVG-Medaille mit lucide-Symbol ist weg, `wappen.tsx` wird nur noch für Schrift und Band der Wappen gebraucht. Das Band läuft am oberen Bildrand aus (so erzeugt), fällt in den Größen 64–80 px nicht auf.
- 36 AUSZEICHNUNGEN (9. September 2026, Auftraggeber: "die bleiben für immer, also brauchen wir viele, und sie müssen zu unserer 50-Stunden-Woche Montag bis Sonntag passen"): Gruppen in `AUSZEICHNUNG_GRUPPEN` (besondere, serien, stunden, liga, tage, team, lernen, uhrzeit), jede Auszeichnung hat `gruppe`; `AUSZEICHNUNG_REIHENFOLGE` ergibt sich aus den Gruppen. Neu: serie_6/serie_12 (6/12 Wochen Zielrang), stunden_100/500/1000/2500/5000 (produktive Sekunden über alle Zeit aus der DB-Funktion `produktiv_gesamt()`), liga_bronze … liga_legende (Trophäenstand ≥ Schwelle, Bilder = die Liga-Wappen), ultra (12 h an einem Tag), durchlaeufer (7 Tage in Folge ≥ 4 h, über Wochenwechsel hinweg), wochensieger (abgeschlossene Woche mit den meisten Stunden, mindestens 2 Personen mit Daten), dauersieger (3 in Folge), team_woche (alle Personen mit Daten erreichen ihren Zielrang), lernmeister (4 Wochen alle Lernziele), blitzsauber (4 abgeschlossene Wochen ≥ 20 h ohne Ungeklärtes). Geändert: perfekte_woche = alle 7 Tage ≥ 5 h (vorher Mo–Fr ≥ 8 h), wochenend_krieger = 15 h am Wochenende (vorher 5 h, bei Wochenendarbeit trivial); nach Rückmeldung "Rang 5 ist mega schlecht, wir wollen immer 50 h": dauerbrenner = 4 Wochen in Folge ≥ 55 h (vorher Rang 5), fokus_woche ab Zielrang statt Rang 5, aufgeraeumt/blitzsauber ab 40 h statt 20 h. `auszeichnungenPruefen` bekommt einen `AuszeichnungsKontext` (gesamtProduktiv, trophaeen, teamWochen, teamZiele, eigeneUserId); `src/main/auszeichnungen.ts` holt ihn je Prüfung aus `produktiv_gesamt`, `liga_stand`, `team_wochen` (13 Wochen) und der Tabelle `ziel`; fehlt etwas, fallen nur die betroffenen Auszeichnungen aus. Das Prüffenster bleibt 12 Wochen zurück plus die laufende (13), reicht für serie_12. DB: `supabase/13_auszeichnungen_erweitert.sql` (Check-Constraint mit allen 36 Typen plus `produktiv_gesamt()`, security invoker, ohne Testdaten, plus `liga_stand()` mit Urlaub Mo–So; vom Auftraggeber am 10. September 2026 selbst im SQL-Editor ausgeführt und aus der App geprüft (liga_stand mit letztes_wirksam, urlaub:alle, Auszeichnungsliste). Die Chrome-Erweiterung war dafür nicht verbunden (Profil "wessamedia.com" zeigt in der Erweiterung "Anmelden"), der Supabase-CLI-Login braucht ein echtes Terminal mit Enter-Druck und eine Browser-Bestätigung, beides kann Claude nicht auslösen; Skript per `Set-Clipboard` in die Zwischenablage gelegt und als Datei nach Dokumente → Zeiterfassung App kopiert); ohne das Skript scheitern Inserts neuer Typen still (Liste wird neu geladen). Bilder: 14 weitere Medaillen (28 Credits, gleiche Prompts mit Champion-Wappen als `image_references`; Stundenglas in Bronze/Silber/Gold/Kristall/Diamant, Silberringe, Eisenkette, Doppelgipfel mit Polarlicht, Pfad mit Fußspuren, Krone im Lorbeer, drei Kronen, drei Schilde, Buch mit Hut, Kristallschild mit Haken), Liga-Auszeichnungen nutzen `liga-*.png`. `Auszeichnungen.tsx` zeigt die Gruppen mit Zähler "3/8", Raster bis 8 je Zeile, Medaillen 62 px.
- WOCHENRÜCKBLICK als Vollbild (9. September 2026, "wie ein Saisonende"): `Wochenrueckblick.tsx` in `App.tsx` (Fehlerschutz). Erscheint einmal je abgeschlossener Woche, sobald ab Montag das Fenster sichtbar ist (Prüfung beim Start, bei visibilitychange und alle 30 min); gemerkt in localStorage `rueckblick.gezeigt.<uid>` = Montag der gezeigten Woche; Wochen ohne einen einzigen bewerteten Block werden nur gemerkt, nicht gezeigt. Daten: Blöcke der Vorwoche per `bloecke:zeitraum`, `wochenstatistik`, Rang, Ziel-Rang, Auszeichnungen mit `wocheStart` = Vorwoche, Liga aus `liga:stand` (nur wenn `letzteWoche` = Vorwoche: Delta, wirksames Delta, Stand), Platz im Team aus `team:wochen(vorwoche, vorwoche)`. Aufbau mit den Aufstiegs-Klassen (Strahlenkranz, Druckwelle, Funken in der Stufenfarbe des erreichten Rangs): Titel, KW und Zeitraum, Wappen 170 px, "Rang N · Name", "Wochenziel geschafft" oder "Ziel war Rang X", Kacheln (Produktiv, Unproduktiv, Bester Tag, Trophäen, Team) gestaffelt, darunter die neuen Auszeichnungen als Medaillen, Knopf "Auf in die neue Woche". Ton `auszeichnung` beim Erscheinen. Test in der Entwicklungsversion: `rueckblickTest()` (erzwingt die Vorwoche). Die kleine Karte `Wochenzusammenfassung` auf der Woche bleibt bestehen.
- TEAM-PROGNOSE (9. September 2026): `LigaRangliste` lädt zusätzlich `team:stand`, `ziele:alle` und das neue `urlaub:alle` (Tabelle ist für alle lesbar; `alleUrlaube` in `src/main/urlaub.ts`) und zeigt je Person unter den Trophäen "voraussichtlich +40" (gleiche `wochenPrognose` und `wirksamesDelta` wie die eigene Karte; am Wochenende "diese Woche", montags vormittags "Prognose ab Montagmittag", ganze Urlaubswoche "diese Woche Urlaub") und darunter "letzte Woche ±0" (wirksames Delta).
- LIGA-AUFSTIEG groß (9. September 2026): `RangAufstieg.tsx` feiert jetzt auch Ligen (`Feier` = rang | liga); die Liga-Karte ruft `ligaAufstiegZeigen(trophaeen)` (CustomEvent `liga-aufstieg`), wenn der gemerkte Liga-Index gestiegen ist, Abstieg bleibt eine kleine Meldung. Liga-Wappen 230 px, Farbe aus LIGA_FARBEN, Texte "Liga-Aufstieg" / Liganame / Trophäen. Test in der Entwicklungsversion: `ligaAufstiegTest(1400)`.

## Design
Hintergrund #0B0B0C, Flächen #151517, Text #F2F2F3, Nebentext #8E8E93. Grün #00C076 produktiv, Rot #FF4D4D unproduktiv, Grau #3A3A3E inaktiv, Hellgrau #8A8A8F ungeklärt. Ecken 16 px. Schrift Inter (gebündelt über @fontsource-variable/inter), Tabellenziffern überall. Keine Rahmen, keine Schatten, keine Verläufe. Inhalt als mittige Spalte, maximal 800 px breit, Navigation unten.
Tailwind-Farbnamen: bg-ground, bg-panel, text-ink, text-mute, text-dim, produktiv, unproduktiv, inaktiv, ungeklaert, orange. Rundungen: rounded-card (16 px), rounded-chip (8 px).
Seit 9. September 2026 auf Wunsch des Auftraggebers ("zu schlicht, moderner, Animationen wie am Mac") bewusst lebendiger, die Regel "keine Verläufe, keine Schatten" gilt insoweit nicht mehr: Hintergrund mit weichen Lichtflecken in Orange und Grün, die in 26 s langsam atmen (`body::before`), darüber feines Korn als SVG-Rauschen (`body::after`, 5 %). Karten als Milchglas (`.glas`: Panelfarbe zu 80 % plus Lichtkante oben, bewusst OHNE backdrop-blur, weil Blur die Übergänge ruckeln lässt). Navigation mit gleitender Markierung (`Navigation.tsx`, Breite 96 px + 4 px Abstand). Screen-Wechsel als Kreuz-Übergang in `App.tsx` (Stand 9. September 2026, zweite Fassung): Screens bleiben nach dem ersten Besuch dauerhaft geladen (`besucht`), jeder in einem eigenen Scroll-Container `absolute inset-0 overflow-y-auto` unter `main`, nicht aktive per `hidden`; so behält jeder Screen seine Scroll-Position, und beim Wechsel wird nichts neu aufgebaut, keine Diagramme animieren. Der alte Screen (`abgang`) gleitet 260 ms in Klickrichtung hinaus und blendet aus, der neue gleitet von der Gegenseite herein (460 ms; display none → block startet die Animation neu); Richtung aus `SCREEN_REIHENFOLGE`; ein noch nie besuchter Screen wird per `startTransition` aufgebaut. Dialoge (`BlockDialog`, `RangUebersicht`, `LigaUebersicht`, `SymbolWahl`, `RangAufstieg`) hängen über `Portal.tsx` (`createPortal`) am body und haben `overscroll-contain`: vorher lagen sie im DOM innerhalb des scrollenden Screens, und Scrollen im Dialog reichte an die Seite durch, sodass man nach dem Speichern am Listenende landete. Dialoge blenden auf und federn herein (`animate-aufblenden`, `animate-einblenden`), Knöpfe und klickbare Zeilen (`.druckbar`) federn beim Drücken auf 96 %. HAUPT-KNOPF `.knopf-primaer` (10. September 2026, "Knöpfe offensichtlicher, grün, leuchtend"): grüne Fläche, dunkle Schrift, atmendes Leuchten (`knopf-atmen` 2,8 s, beim Hover ohne Atmen, heller; seit 10. September 2026 atmet nicht der box-shadow, sondern die Deckkraft eines `::after`-Scheins, weil die box-shadow-Animation jedes Bild auf dem Hauptthread neu zeichnete: Leerlauf 61 → 3 Hauptthread-Frames je Messfenster); für die wichtigen Wege: "Alle 15 Ränge ansehen" (Woche), "So funktioniert die Liga" (Liga-Karte), "Woche durchgehen", "Jetzt durchgehen" (Wochenzusammenfassung), "Einzeln durchgehen" und "Alle N zuordnen" (Postfach), "Alle Ränge" (Team). Alles andere bleibt grau (`bg-panel-2`) oder weiß (Speichern in Dialogen). LEUCHTEN ÜBERALL (9. September 2026, "das Rot muss so leuchten wie das Grün"): globale CSS-Regeln in `styles.css` über Attribut-Selektoren `.recharts-surface [fill='#00C076']` / `#FF4D4D` / `#FE5303` (und `[stroke=…]`) geben allen Diagrammteilen in den App-Farben einen drop-shadow, `.bg-produktiv/.bg-unproduktiv/.bg-orange` einen box-shadow, `.text-produktiv/...` einen text-shadow; der Verteilungsring glüht je Anteil in seiner Farbe (inline). Neue Farben in Diagrammen bitte exakt in Großschreibung wie die Tokens angeben, sonst greift der Selektor nicht. HINTERGRUND mit Spiel-Charakter (`Hintergrund.tsx`, in `App.tsx` als erstes Kind, `position: fixed; z-index: -1`): treibendes Instrumenten-Raster mit radialer Maske, zwei langsam drehende Zifferblatt-Ringe mit Skalenstrichen und je einem farbigen Bogen (oben rechts orange, unten links grün), 28 aufsteigende Lichtpunkte (fester Zufall, nur transform/opacity). Tages-Ring: produktiver Anteil glüht (drop-shadow auf der Recharts-Cell), Rang-Ring ebenso; dafür `.recharts-surface { overflow: visible }` und `overflow="visible"` am eigenen SVG, sonst wird das Glühen am Rand abgeschnitten und es entsteht ein sichtbares Quadrat. Kurve überall `--ease-federnd`. `prefers-reduced-motion` schaltet alle Animationen ab.
HINTERGRUND LOGO (10. September 2026, Auftraggeber: "das Logo als Hintergrund probieren, mit orangenen Linien, die durch die Schrift fahren; falls es nicht klappt, wieder zurück"): zwei Fassungen in `Hintergrund.tsx`, umschaltbar unter Einstellungen → Darstellung → Hintergrund (`src/renderer/src/hintergrundart.ts`, localStorage `hintergrund`, Standard `logo`; `klassisch` ist der bisherige Hintergrund unverändert, `HintergrundKlassisch`). Logo-Fassung (`HintergrundLogo`): hinten (z-index −1) das schwächere Raster, das Linienmuster des Logos frei nachempfunden (`MUSTER`, zwölf Linienzüge im Raster 1000 × 600, oben und unten, Weiß 11 %) mit fünf orangenen Lichtketten auf ausgewählten Linienzügen (`MUSTER_KETTEN`, hin und zurück) und 30 Lichtpunkte nur in Orange und Weiß; vorn (z-index 10, pointer-events none, unter Mehrfachleiste z-30 und Dialogen z-40) die Wortmarke "wessamedia" als Wasserzeichen mittig, Breite min(78vw, 1150px), Weiß 4 % Füllung plus 7 % Kontur, darauf vier orangene Lichter, die in 40 s von links nach rechts AM RAND der Schrift entlanglaufen (dritte Fassung, 10. September 2026, drei Rückmeldungen in Folge: erst kreisten Ketten um alle Konturen mit Sprüngen ("schwebt hin und her"), dann lief eine von Hand gezeichnete Mittellinie durch die Buchstaben ("nicht durch die Mitte, außen am Rand"); jetzt baut `wortmarkeUmriss()` den Weg aus den Konturen selbst: nur die äußeren Umrisse (Innenflächen liegen im Kasten eines anderen), nach x sortiert, jeder beginnt an seinem linkesten Punkt und läuft im Uhrzeigersinn, wie der Nachzeichner ihn geliefert hat, dann noch einmal oben entlang bis zum rechtesten Punkt (anderthalb Runden), damit das Licht den Buchstaben rechts verlässt und der Sprung zum nächsten nur die kleine Lücke ist; vorher endete jeder Buchstabe links und der Sprung zog sichtbar quer über den Buchstaben, "komisch rübergezogen"). Lichter mit `richtung: 'normal'` laufen immer in eine Richtung: der Kopf läuft um eine Kettenlänge über das Bahnende hinaus, Glieder vor dem Anfang und hinter dem Ende sind unsichtbar, dazwischen blenden sie auf den ersten und letzten 6 % der Bahn (höchstens 80 Einheiten) per Deckkraft-Keyframes ein und aus; so verlässt die ganze Kette die Bahn, statt am Ende stehen zu bleiben und dort zu verblassen (Rückmeldung "bleibt stehen und verschwindet langsam" bei den Querstrichen; vorher Modulo-Umlauf). Wortmarke 64 s je Durchlauf (40 s "zu schnell"). Lichtpunkte in der Logo-Fassung 36 Stück, 25 % größer, mit weichem Hof (`Lichtpunkte hof`: Kind-Element `.hintergrund-hof`, radialer Verlauf in `--farbe`, OHNE eigene Animation) und Funkeln über zusätzliche Deckkraft-Keyframes in `punkt-aufsteigen`. Befund: jede Animation an einem Kind oder Pseudo-Element eines aufsteigenden Punkts lief auf dem Hauptthread (33–41 statt 4–6 Frames je Messfenster, egal ob nur Deckkraft oder nur Skalierung, per CDP mit eingeschleusten Styles gemessen), deshalb steckt das Funkeln in der Animation des Punkts selbst. Die Lichter sind PERLENKETTEN (`Perlen`: runde Punkte, vorne hell, nach hinten verglühend wie ein Komet; Schrift 30 × 6 px im Abstand 3,5 px, Muster 26 × 7 px im Abstand 4 Rastereinheiten), weil sich starre gedrehte Striche an den spitzen Zacken sichtbar aufteilten; Linie und Licht des Musters nutzen DENSELBEN Pfad (`eckenAbrunden`, Radius 16, sichtbar mit runden Kappen), weil das Licht sonst an den Zacken neben der Linie lief ("läuft nicht exakt über die Striche"); alle zwölf Linienzüge haben ein Licht, auch die Querstriche, jedes mit `pause` (Anteil der Laufzeit, in dem das Licht nach dem Ausblenden unsichtbar wartet, Keyframe-Offsets gestaucht plus Halte-Keyframe; Querstriche 0,65, lange Zacken 0,3–0,35), damit die Querstriche nur ab und zu leuchten und das Muster nicht zu voll wird; Laufzeiten 10–15 s inklusive Pause. Die klassische Fassung behält ihre gedrehten Pillen (`Glieder`). HEUTE-ABSTAND (10. September 2026, "oben klar, unten verblasst, nervt"; der Auftraggeber wählte diese Alternative statt der Wortmarke als Seiteninhalt): `HeuteScreen` misst per `useLayoutEffect` (und bei `resize`), wo der Bereich unter dem Ring ungescrollt beginnt, und schiebt ihn per `marginTop` unter die Unterkante der Wortmarke (`wortmarkeLage(innerWidth, innerHeight)` in `wortmarke.ts`, gleiche Breitenregel wie das CSS min(78vw, 1150px)) plus 14 px; bei klassischem Hintergrund 0. So liegt in der Grundstellung keine Karte über der Schrift (723 px Fensterhöhe: 34 px, 950 px: 148 px), beim Scrollen wird das Licht hinter den Karten weiter gedämpft (vom Auftraggeber akzeptiert). Emulierte Fenstergrößen zum Prüfen: Scratchpad `emulieren.cjs breite hoehe | aus` (Emulation.setDeviceMetricsOverride; `Browser.setWindowBounds` gibt es am Electron-Endpunkt nicht). Seit der Rückmeldung "die orangenen Linien ziehen über den grünen Halbkreis und über die Schrift" (10. September 2026) liegen ALLE Lichter hinten (z-index −1, hinter Ring, Karten und Text), auch die am Rand der Wortmarke; nur die Wortmarke selbst bleibt vorn. Die Wortmarke liegt bewusst ÜBER den Karten, weil sie hinter dem Milchglas (80 %) unsichtbar wäre; dadurch laufen die Ketten auch über Text (so gewünscht: "durch die Schrift fahren"), bei Bedarf Deckkraft in `HintergrundLogo` anpassen. Die Wortmarke selbst: `components/wortmarke.ts` (`WORTMARKE`, Raster 921 × 110, 13 Konturen, fill-rule evenodd), nachgezeichnet aus `C:\Users\mouga\Downloads\wessamedia logog.jpg` (identisch mit dem im Chat eingefügten Bild; `logo wessamedia.png` daneben ist dieselbe Wortmarke kleiner) mit dem Scratchpad-Skript `logo-nachzeichnen.cjs` (Electron nativeImage: weiße Pixel > 215 je Kanal, gerichtete Randkanten im Uhrzeigersinn verfolgen, Douglas-Peucker mit 1,8 px; `svg-foto.cjs` rastert ein SVG zur Begutachtung, `bild-extrahieren.cjs` holt ein im Chat eingefügtes Bild aus dem Sitzungsprotokoll). KEYFRAMES IN PFAD-EINHEITEN, MASSSTAB PER TRANSFORM (10. September 2026, "beim Maximieren hängt es kurz"): Vorher berechnete der ResizeObserver bei jeder Größenänderung alle Keyframes in Pixeln neu (hunderttausende getPointAtLength-Aufrufe, spürbarer Hänger beim Maximieren). Jetzt werden die Keyframes einmal in den Einheiten des Pfads gebaut, die Perlen sitzen in Behältern mit fester Einheiten-Größe (Muster 1000 × 600, Wortmarke 921 × 110), die `skalieren(el, sx, sy, dx, dy)` per CSS-Transform auf das Fenster bzw. auf die Lage der vorn gezeichneten Wortmarke legt; bei Größenänderung ändert sich nur diese Transform. Die Glieder skalieren mit dem Behälter (ein Versuch, sie per CSS-Variablen und `calc()` gleich groß zu halten, kostete beim Maximieren 190 ms Style-Neuberechnung, gemessen mit Scratchpad `spur-fenster.cjs`, das das Fenster per Win32 `ShowWindow` über `fenster.ps1` wirklich maximiert und dabei eine Chrome-Spur aufzeichnet). ZWEITE RUNDE (gleicher Tag, "hängt immer noch, und die Lichter sind gepunktet"): Die Spur zeigte den Compositor mit 371 Animationen fast ausgelastet (TickAnimations rund 12 ms je Bild, `AnimationHost::PushProperties` 110 ms je Commit); deshalb jetzt nur noch 114 Glieder: alle zwölf Musterlinien liegen als Teilstücke in EINEM Pfad (`musterWeg`, d = gerundete Linien verbunden), drei Lichter (`MUSTER_LICHTER`, 36 s Umlauf, gleichmäßig versetzt) laufen ihn ab, `Kette.grenzen` (Längen, an denen die Teilstücke enden) lässt sie an jedem Linienende aus- und am nächsten Anfang einblenden, der Sprung bleibt unsichtbar; die Wortmarke hat drei statt vier Lichter. DRITTE RUNDE, LEINWAND (gleicher Tag): Auch mit 114 Gliedern blieb der Compositor bei 58 % (TickAnimations 1,6 s je 2,8 s), darum zeichnet die Logo-Fassung ihre Lichter jetzt auf ein `<canvas>` (`lichterZeichnen`, requestAnimationFrame, je Bild rund 400 kurze Striche in drei Durchgängen breit/mittel/Kern als Schein, Komet-Verlauf, nur die Flecken des letzten Bilds werden gelöscht, ruht bei verborgenem Fenster, DPR-bewusst); die Bahnen bleiben abgetastete Tabellen in Einheiten, `Licht.abbilden` liest die aktuellen Maßstäbe (`mass`), die `anpassen` bei Größenänderung nur überschreibt. Gemessen: Hauptthread je Bild rund 0,9 ms (`zeichnen` 125 ms je 3,5 s), Maximieren jetzt 83/54 ms lange Aufgaben statt 155–186 ms, gleichauf mit Klassisch (90/61/51); der Rest ist Style-Neuberechnung und React der sechs vorgebauten Screens. Außerdem `punkt-aufsteigen` mit fester Strecke −1300 px statt −105vh: Keyframes mit Fensterhöhe wurden bei jeder Größenänderung für alle Punkte neu aufgelöst (rund 80 ms). `Glieder` (gedrehte Striche, `komet`) und `kettenStarten` bleiben für die klassische Fassung; `Kette.grenzen` ist dort ungenutzt. Messwerkzeuge: `spur-fenster.cjs` (Spur bei echtem Maximieren), `profil-fenster.cjs` (CPU-Profil dabei), `fenster.ps1 -Befehl 3|9`. FEINSCHLIFF (gleicher Tag, "immer noch leicht gepunktet, oben und unten ein paar Linien mehr, in der Schrift länger und ein, zwei mehr, Punkte etwas größer"): Die Kette wird je zusammenhängendem Lauf in EINEM Zug gestrichen (Einzelstücke mit runden Enden überlagerten sich an den Nähten und wirkten gepunktet), der Komet-Verlauf kommt aus einem `createLinearGradient` vom Kopf zum Schwanz mit einer Stufe je Punkt (auf die Kopf-Schwanz-Linie projiziert, monoton geklemmt); vier Musterlichter (40 s Umlauf), fünf Wortmarkenlichter mit 44 statt 22 Gliedern, Lichtpunkte Faktor 1,5 und Hof inset −140 %. MUSTER 1:1 AUS DEM LOGO (gleicher Tag, "bitte eins zu eins nachbauen, oben und unten passen die Linien nicht zusammen", danach "mehrere Striche übereinander, nicht wie die Abbildung"): Die erste Fassung per Hough-Transformation (Scratchpad `muster-nachzeichnen.cjs`) lieferte Bruchstücke, doppelte Parallelen und Haarnadeln an Kreuzungen. Endgültig ist `muster-skelett.cjs` (Electron nativeImage, Quelle `C:\Users\mouga\Downloads\wessamedia logog.jpg` 1440 × 753): Maske über die HELLIGKEIT (0,299 r + 0,587 g + 0,114 b > 160, nicht weiß, ohne das Wortmarken-Band y 295–465; eine Farbmaske zerfiel in Punkte, weil die JPEG-Farbunterabtastung dünne Linien entfärbt, die Linien sind 7 px breit und hell), Ausdünnen auf 1 px (Zhang-Suen), Knoten über die ÜBERGANGSZAHL (0→1-Übergänge im Kreis um den Pixel: 1 Ende, 2 Linie, ≥ 3 Verzweigung; die bloße Nachbarzahl zerhackte schräge Linien an jeder Treppenstufe in Stücke), Ketten von Knoten zu Knoten verfolgen, Douglas-Peucker 2,2 px, Ketten unter 12 px und Segmente unter 8 px verwerfen, kollineare Segmente mit nahen Enden mehrfach zusammenlegen (Enden ≤ 20 px, Winkel ≤ 5°, bei Stücken unter 40 px ≤ 14°, Fortsetzung ≤ 4 px neben der Geraden des längeren Stücks), Reste unter 14 px und Stücke entlang des Bildrands verwerfen, nahe Enden (≤ 10 px) auf ihren Schwerpunkt ziehen, Enden nahe der Mitte eines anderen Segments (≤ 7 px, T-Knoten) genau darauf setzen, nur FREIE Enden nahe am Bildrand (24 px) um 40 px hinausziehen (verbundene Enden nicht, sonst ragten die Schenkel der A-Form über ihre Spitze), ins Raster 1000 × 523 umrechnen, Ecken (genau zwei Enden, kein drittes Segment hindurch) zu Linienzügen ketten, jeden Zug so drehen, dass er an dem Ende beginnt, das weiter von der Wortmarke weg liegt (die Lichter laufen von außen zur Schrift). Ergebnis: 28 Linienzüge in `MUSTER`; Kontrollbilder `muster-skelett-kontrolle.svg/.png` (grün über dem Logo), `muster-skelett-rein.svg/.png` (nur die Linien auf Dunkel, deckungsgleich mit dem Logo), `muster-skelett-roh.svg` (Ketten vor dem Zusammenlegen), `logo-zoom.cjs` schneidet sechs vergrößerte Ausschnitte aus dem Logo, `pixel-dump.cjs` zeigt Helligkeiten um einen Punkt. Die Ecken-Rundung (`eckenAbrunden`, Radius 16) ist entfernt: Linien und Lichter sind spitz wie im Logo. SEITENRICHTIG: Das Muster hat sein eigenes Raster 1000 × 523 (`MUSTER_BREITE/HOEHE`, das Logo-Bild 1440 × 753 gleichmäßig skaliert; Umrechnung der zuerst auf 1000 × 600 verzerrten Koordinaten per Scratchpad `muster-raster.cjs`), das SVG nutzt `preserveAspectRatio="xMidYMid slice"` und die Leinwand dieselbe Abbildung (`mass.sx = mass.sy = max(Breite/1000, Höhe/523)`, mittig per `mass.dx/dy`): gleichmäßig auf Fensterhöhe oder -breite gezogen, Überstand abgeschnitten, Winkel wie im Logo. Die klassische Fassung bleibt bei 1000 × 600 mit "none". ORANGENES AUFBLITZEN behoben (Strich quer über den Bildschirm zwischen dem Ende einer Musterlinie und dem Anfang der nächsten): `abtasten` sucht in jedem Abtastintervall mit einem Sprung (Abstand zweier Abtastpunkte > 5 Einheiten) die genaue Sprungstelle per Bisektion (16 Schritte; ein Punkt gehört noch zur alten Linie, solange sein Luftweg zum letzten sicheren Punkt nicht größer ist als der Längenabstand) und merkt sich Länge, Punkt davor und Punkt danach (`spruenge`); `bei` interpoliert bis exakt ans Linienende bzw. ab dem Linienanfang. Ein erster Versuch, nur auf den näheren Abtastpunkt zu springen, ließ den Strich stehen, weil ein Punkt vor der Sprungstelle auf die neue Linie rutschte und die Lauf-Trennung nach `teil` ihn nicht erwischte. Nachweis per Pixelprüfung im Scratchpad: `licht-pruefen.js` (per `eval.cjs`) malt alle Muster- und Wortmarken-Pfade mit 14 px Breite in eine Maske und zählt 70 s lang alle 200 ms die leuchtenden Leinwand-Pixel außerhalb der Maske (vorher bis 5 500 je Bild); `licht-bild.cjs` speichert das schlimmste Bild als `licht-falsch.png` (Maske grau, Licht orange, Fehler magenta); nach dem Umbau 0 Fehlpixel in 329 Bildern. LICHTER MIT ABLAUFPLAN (gleicher Tag, "die Lichtstreifen müssen auch verbessert werden"): `Licht` hat `teile` (Teilstücke der Bahn; beim Muster aus den Sprüngen der Abtastung, `teileAusSpruengen`), einen `plan` aus Läufen (welches Teilstück von welcher bis welcher Sekunde, `planBauen`), `zyklus`, ein festes `tempo` in Einheiten je Sekunde (Muster 150, Wortmarke 90; lange Linien dauern länger, kurze Querstriche blitzen nicht mehr nur Zehntelsekunden auf wie beim alten Ein-Pfad-Umlauf in fester Zeit) und `blende` in Einheiten (Muster 14, Wortmarke 12). Muster: `MUSTER_LICHTER` 6 Lichter, die 28 Linien fest gemischt (`mischen`, Saat 23) reihum verteilt, jedes Licht fährt seine Linien nacheinander mit festen Zufallspausen 0,8–2,4 s ab (im Schnitt drei bis vier Lichter gleichzeitig); nie zwei Lichter auf derselben Linie, alle Zyklen verschieden lang, kein Takt. WORTMARKE: ZWEI RAND-BAHNEN, OBEN UND UNTEN (gleicher Tag, vierte Rückmeldung zur Schrift; Verlauf der Rückmeldungen: (1) Ketten um jeden Buchstaben mit 1,5 Runden "hängen, gehen zurück, teilen sich auf"; (2) obere und untere SILHOUETTE (Spalten-Hüllkurve, Kerben gefüllt) "läuft nur oben und unten, nicht durch die ganzen Ränder"; (3) EIN durchgehender Weg, jeder Buchstabe eine volle Runde plus Zusatzstrecke oben und gerade Brücke zum nächsten (`wortmarke-lichtweg.cjs`) "bei w, e, a werden die Linien nicht ganz mitgenommen [die Schlitz-Regel, Sehne ≤ 8, kappte die V-Spitzen des w und den Schlitz unter dem Balken des a], die Übergänge gehen nicht, das andere Ende wandert zurück [eine volle Runde läuft unten zwangsläufig nach links]"): Endgültig `wortmarke-bahnen.cjs` (im Projektordner mit node starten, schreibt `WORTMARKE_BAHN_OBEN` und `WORTMARKE_BAHN_UNTEN` in `wortmarke.ts` und `wortmarke-bahnen-kontrolle.svg`): äußere Konturen nach x sortiert, jede im Uhrzeigersinn (positive Schnürsenkel-Fläche bei y nach unten), an ihrem LINKESTEN und RECHTESTEN Punkt geteilt (bei Gleichstand der obere); die obere Bahn läuft vom linkesten Punkt im Uhrzeigersinn bis zum rechtesten (oben entlang), die untere vom rechtesten weiter bis zum linkesten und wird umgedreht, also auch links → rechts. Zusammen sind die beiden Bahnen exakt der komplette äußere Rand (Öffnungen von e, s und a, Zacken von w und m, Spalt des d), NICHTS überbrückt oder vereinfacht; lokale Rückläufe gibt es nur innerhalb einer Öffnung (z. B. in der Öffnung des e, gewollt: "durchs ganze e"). Je Buchstabe ein Teilstück (M), keine Brücke: das Licht blendet über `blende/2` (8 Einheiten) am Buchstabenende aus und am nächsten Anfang ein (`sprungNachbarn`, Läufe brechen am Sprung). Längen 2404 (oben) und 2739 (unten); seit 11. September 2026 ("ein paar mehr Lichtstreifen, durchgehender beleuchtet") 5 Lichter oben, 4 unten (halbe Lücke versetzt), 48 Glieder (vorher 3 + 2 mit 44), 90 Einheiten je Sekunde, `blende` 16; damit sind rund 30 % der Ränder gleichzeitig beleuchtet statt 15 %. Die geschlossenen Innenflächen (Löcher der beiden e und des d) sind seit 11. September 2026 ("beim e gibt es einen Teil, der gar nicht abgefahren wird") je eine volle Runde im Uhrzeigersinn ab ihrem linkesten Punkt, als eigenes Teilstück direkt nach dem Buchstaben in der Bahn, in deren Hälfte das Loch liegt (e-Löcher oben, d-Loch unten); Längen seitdem 2656 (oben) und 2915 (unten), 13 Teilstücke je Bahn. Kontrolle: `wortmarke-bahnen-kontrolle.png` (oben grün, unten blau, linkester Punkt weiß, rechtester orange; Ausschnitte `zoom-bahnen-links/rechts.png`), in der App `bahnen-zeigen.js`. Beim Umbau auf den durchgehenden Weg hatte `umbau2.cjs` versehentlich `Lichtpunkte`, `partikelErzeugen`, `bewegungReduziert` und `HintergrundKlassisch` mit entfernt (Marker zu weit gefasst); `wiederherstellen.cjs` holte sie aus der Sicherung `Hintergrund.vorher.tsx` zurück. HELLIGKEIT ENTLANG DER BAHN (gleicher Tag, fünfte Rückmeldung: "der hintere Teil, wo das Helle verblasst, läuft wieder zurück; die Linie muss in einem Stück durchlaufen"): Ursache war der Komet-Verlauf als `createLinearGradient` entlang der SEHNE Kopf → Schwanz (jeder Punkt auf die Sehne projiziert): in jeder Ecke eines Buchstabens drehte sich die Sehne, die Helligkeitsbänder stehen senkrecht zur Sehne, und die Grenze, an der das Licht verblasst, rutschte auf dem hinteren Schenkel sichtbar nach hinten; auf den geraden Musterlinien trat das nie auf. Jetzt zeichnet `lichterZeichnen` je Lauf und Durchgang (breit/mittel/Kern) zehn HELLIGKEITSSTUFEN (`STUFEN` 0,05 … 0,94): für jede Stufe alle Punkte mit a ≥ Stufe als durchgehende Züge (runde Ecken und Kappen, an Blenden auch mehrere Stücke) über die dunkleren Stufen gelegt, Deckkraft je Stufe = 1 − (1 − Ziel)/(1 − vorheriges Ziel), sodass die Gesamtdeckkraft genau die Stufe × Durchgangsstärke erreicht; Überlappungen innerhalb einer Stufe gibt es nicht (ein Zug je Stück), darum keine "gepunkteten" Nähte wie bei Einzelsegmenten. Die Helligkeit folgt damit exakt der Bahn. Bildfolgen zum Prüfen: Scratchpad `folge.cjs name x y b h anzahl abstandMs` (CDP-Fotos eines Ausschnitts in CSS-Pixeln, gestapelt zum Kontaktbogen; die Lage der Wortmarke per `getBoundingClientRect` abfragen, in 986 × 723 etwa 109/316/769/92), `folge-links.png` zeigt das untere Licht durch das e und seine Öffnung.`licht-bild.cjs` speichert das schlimmste Bild als `licht-falsch.png` (Maske grau, Licht orange, Fehler magenta). Außerdem: Bahnen werden einmal alle 2 Einheiten abgetastet (`abtasten`, `bei` interpoliert linear) statt je Keyframe dreimal getPointAtLength, Schritte = Strecke/10 (höchstens 1000), und Keyframes werden je Pfad und Glied zwischengespeichert (`keyframeSpeicher`), sodass die vier Wortmarken-Lichter dieselben Arrays nutzen. Gemessen: 250 Perlen, rund 290 Animationen, alles auf dem Compositor.
Prüfen ohne Bildschirm: `npx electron-vite dev -- --remote-debugging-port=9222` im Hintergrund, dann ein CDP-Skript (Runtime.evaluate, `Page.captureScreenshot`) liefert echte Bildschirmfotos der App, auch bei gesperrtem Windows; `document.getAnimations()` zeigt laufende Animationen mit Zeiten. Abzeichen groß begutachten: die SVGs aus dem Übersichts-Dialog per Skript in ein festes Galerie-Div klonen und fotografieren.
Rang- und Liga-Abzeichen (Stand 9. September 2026, vierte Runde: drei SVG-Runden waren dem Auftraggeber "immer noch nicht cool genug", darum mit seiner Zustimmung KI-Bilder): je Stufe ein illustriertes Wappen, erzeugt über das Higgsfield-MCP des Auftraggebers (Modell `nano_banana_pro`, 2 Credits je Bild, 13 Bilder = 26 Credits; das Champion-Wappen, Job 87304968-2dc8-4898-bb34-541afd3ab267, war die Stilreferenz für die anderen zwölf). Die Quellbilder (1024 px, fast schwarzer Grund) liegen nur im Scratchpad der Sitzung, nicht im Projekt. `wappen-verkleinern.cjs` (läuft mit `node_modules.binelectron`, nutzt `nativeImage`, kein Zusatzpaket) verkleinert auf 384 px und rechnet den Alphakanal: Flutfüllung vom Bildrand über dunkle Pixel (hellster Kanal < 48) ergibt "außen", dort Schwarzwert 30 abziehen und Alpha = hellster Kanal (das Leuchten bleibt weich), alles andere voll deckend. Ohne den Schwarzwert-Abzug blieb ein grauer Kasten um jedes Wappen (der Grund der Quellen liegt bei 18–31, nicht bei 0); der erste Versuch mit JPEG plus `mix-blend-mode: screen` zeigte ebenfalls einen dunklen Kasten, weil der Screen-Wrapper einen eigenen Stapelkontext bildet. Ergebnis: `src/renderer/src/assets/wappen/rang-{bronze,silber,gold,champion,diamant}.png` und `liga-{bronze,silber,gold,kristall,meister,champion,titan,legende}.png` (je 140–240 kB, werden ins Bundle eingebaut). `RangAbzeichen.tsx` und `LigaAbzeichen.tsx` zeigen das Bild als `<img>` um 12 % über den Rahmen hinaus (für das Leuchten) und legen als SVG (Raster 100 × 100) darüber: dunklen Rückhalt (schwarz 38 %), Zahl bzw. römische Ziffer (`Metallschrift`), beim Rang Sterne (Bronze–Gold) oder kleine Brillanten (Diamant) bei y 78 und das Namensband (`Band`, y 86); ohne Rang bzw. Liga ein graues Schild mit 0 bzw. Strich. Aus `wappen.tsx` werden dafür nur noch `Definitionen`, `Metallschrift`, `Band`, `SCHILD`, `dunkel` und `stern` gebraucht; die übrigen Bausteine (`Metallform`, `Lorbeer`, `Fluegel`, `Krone`, `Strahlen`, `Nieten`, `Funkeln`, `Edelstein`, `Brillant`, `Flammen`) bleiben für `AuszeichnungBild` und spätere Ideen. Größen: Woche-Ring 64, Team-Karte 64, Aufstiegs-Einblendung 160, Wochenzusammenfassung 56, Übersichten 52, Rangliste 40. Dialoge begrenzen ihre Höhe mit `max-h-[calc(100vh-3rem)]`, weil `max-h-full` im zentrierten Flex-Container nicht griff und die Übersichten nicht scrollbar waren. Zum Begutachten klont `galerie.cjs` (Scratchpad) alle `[role=img]`-Abzeichen aus den Übersichts-Dialogen vergrößert in ein Div und fotografiert es.
FLÜSSIGE ÜBERGÄNGE (9. September 2026, dritte Fassung, "noch flüssiger"; per CDP gemessen statt geraten): Vorher fielen beim Wechsel einzelne Bilder auf 100–140 ms, auch bei schon besuchten Screens. Ursachen laut Chrome-Spur: (1) die drehenden Zifferblatt-Ringe waren `<svg>`-Elemente mit CSS-rotate, und Transformationen an SVG laufen in Chromium auf dem Hauptthread, also jedes Bild ein Hauptthread-Frame; (2) die Lichtbahnen animierten `stroke-dashoffset` mit `filter: drop-shadow`, also jedes Bild ein Weichzeichner über die ganze Fläche; (3) jeder Klick in der Navigation renderte alle sechs Screens samt Diagrammen neu (94 ms React-Arbeit); (4) der Heute-Screen renderte per `useSekundentakt` jede Sekunde die komplette Blockliste (rund 50 ms). Behoben: Ringe in ein gedrehtes div gelegt; Funken als Kette aus 10 kurzen Gliedern (`.hintergrund-funke`, 30 × 5 px, Glühen per box-shadow), die `Hintergrund.tsx` per Web-Animations-API (`el.animate`, nur transform, alternate) entlang der mit `getPointAtLength` abgetasteten Bahn bewegt, jedes Glied 13 Rasterlängen hinter dem vorigen, damit sich die Kette mit der Kurve biegt (der Auftraggeber wollte die Wellenform zurück, gerade Pillen sahen "blöd" aus); Farben durchgehend ohne weißen Kern, Grün #00C076 wie der Tages-Ring, Orange #FE5303, Lila #A78BFA wie Leon im Team (Rückmeldung vom 9. September 2026: "nur grün, rot-orange, lila statt blau, kein Weiß in der Mitte"); bei Fenstergröße-Änderung per ResizeObserver neu berechnet, bei "Bewegung reduzieren" gar nicht. `App.tsx`: `ScreenInhalt` ist `memo`, versteckte Screens sind `.screen-versteckt` (opacity 0, pointer-events none) plus `inert` statt `hidden`, alle Screens tragen `.screen-ebene` (dauerhaft will-change), damit der Wechsel reines Compositing ist; die übrigen Screens werden ab Start alle 0,9 s vorab aufgebaut, damit auch der erste Klick ohne Aufbau-Ruckler gleitet. Diagramme (`WochenBalken`, `TaetigkeitenListe`, `TagesRing`, `TeamBalken`, `MonatsVerlauf`, `TrendBalken`, `VerteilungsRing`, `HochrechnungKarte`, `Auszeichnungen`) und `BlockZeile` sind `memo` (BlockZeile ignoriert die Identität von onClick; HeuteScreen gibt je Block-Kennung eine stabile Klick-Funktion über Refs). `useTakt(ms)` in `erfassung.tsx`; HeuteScreen tickt nur alle 30 s, der laufende Zähler ist der eigene Baustein `Laufzeit`. Die Statusmeldung `erfassung:status` kommt alle 5 s (TAKT_MS) und rendert alle `useErfassung`-Nutzer, deshalb müssen teure Kinder gemerkt sein. Ergebnis (Entwicklungsversion): Hauptthread-Frames im Leerlauf 56 → 4 je 0,7 s, Wechsel 28–36 → 47–49 Bilder je 0,8 s, React-Arbeit je Klick 94 → 3 ms. Messwerkzeuge im Scratchpad: `messen.js` (rAF-Bildabstände beim Klick), `spur.cjs` (Chrome-Spur: Summen je Thread, Hauptthread-Frames, Style-Invalidierungen mit Stack; Modus `keine` ohne Klick), `profil.cjs` (CPU-Profil mit Eigen-/Gesamtzeit je Baustein), `konsole.cjs` (Neuladen und Ausnahmen sammeln). Eigenheit: Wird ein Export von `function` auf `memo(...)` umgestellt, meldet Fast Refresh "Component is not a function", bis die Seite einmal ganz neu lädt. Choreografie seit der Rückmeldung "noch cleaner" (9. September 2026): wenig Weg (neu 24 px, alt 16 px), Skalierung nur 0,995, der alte Screen ist nach 180 ms weg (RAUS_MS 180), der neue bleibt in den ersten 30 % bei Deckkraft 0,25 und blendet dann in 380 ms auf, damit nie zwei gut sichtbare Screens übereinanderliegen (kein Doppelbild).
RANG-AUFSTIEG groß (9. September 2026, "richtig coole Animation, in unserem Style, mit Verbindung zum Hintergrund"): `RangAufstieg.tsx` zeigt über `Portal` einen Schleier (schwarz 94 % plus Lichtschein in der Stufenfarbe), einen langsam drehenden Strahlenkranz (`.aufstieg-strahlen`, repeating-conic-gradient mit radialer Maske), zwei Druckwellen (`.aufstieg-welle`), 42 Funken mit festem Zufall je Rang (`.aufstieg-funke`, --dx/--dy), drei Lichtbahnen wie im Hintergrund, die einmal per stroke-dashoffset durchs Bild zischen (`.aufstieg-bahn`, 1,5 s, hier bewusst SVG, weil nur kurz), das Wappen (230 px) mit Überschwingen (`.aufstieg-wappen`, cubic-bezier > 1) und pulsierendem Schein, darunter gestaffelt "AUFSTIEG", "Rang N", Name und "Klicken zum Schließen"; schließt nach 5,2 s oder Klick mit 350 ms Ausblenden (`raus`-Zustand), spielt den Ton `aufstieg`. In der Entwicklungsversion lässt sich die Einblendung mit `rangAufstiegTest(7)` aus der Konsole (oder per CDP) auslösen.
TÖNE (9. September 2026, "richtig cleane Sounds"): `src/renderer/src/toene.ts` erzeugt alle Klänge mit der Web-Audio-API, keine Dateien: `tick` (Klick in der gewählten KLICK-ART, weil dem Auftraggeber zwei Fassungen nicht gefielen und Claude nicht hören kann: `KLICK_ARTEN` tock (Standard, Sinus 520→300 Hz plus dumpfer Anschlag), pop (800→420 Hz), tap (fast nur tiefes Rauschen), fein (2200/3300 Hz sehr leise), keiner; Auswahl in den Einstellungen als Chips, Antippen spielt vor und wählt, localStorage `toene.klick`; `klickProbe(art, lautstaerke)`), `wischen` (Screen-Wechsel; zweite Fassung nach "viel zu hoch, macht die Ohren kaputt": tiefpass-gefiltertes Luftrauschen 220→1100→260 Hz über 360 ms mit 90 ms Einschwingen, Pegel 0,032, darunter ein Sinus 260→330 Hz bei 0,018; die erste Fassung war ein Bandpass-Zischen 500→2800 Hz), `oeffnen`/`schliessen`, `erfolg`, `auszeichnung` (vier hohe Töne aufwärts mit Hall), `aufstieg` (Aufschlag, Wischen, Dreiklang aufwärts, Schlussakkord, Schimmer). Master-Gain plus kleiner Hall (Delay 160 ms, Rückkopplung 0,34, Tiefpass). Einstellungen in localStorage `toene.an` (Standard an) und `toene.lautstaerke` (Standard 0,5), Abschnitt "Töne" in den Einstellungen mit Schalter, Regler und Probehören-Knöpfen (`data-stumm`, damit der Klick-Ton den Probeton nicht überlagert). `klickToeneEinrichten` in `App.tsx` hängt den Klick an `pointerdown` (Capture) für button, [role=button], .druckbar, a, select, Kästchen; höchstens alle 40 ms. `wechseln` spielt `wischen`, neue Auszeichnungen `auszeichnung`. Electron erlaubt AudioContext ohne Nutzergeste (autoplayPolicy Standard), geprüft: state "running".

## Stand
- Schritt 1 (Grundgerüst, leeres Dashboard, dunkles Design): fertig am 8. September 2026. App startet, sechs leere Screens, Navigation unten, Kopfzeile mit Pause-Platzhalter.
- Schritt 2 (Supabase anbinden, Login, Tabellen, Testdaten): fertig am 8. September 2026. Supabase-Projekt "moritz@wessamedia.com's Project" in Organisation "Wessamedia", Projekt-Ref `vgsnjtqcuwrhifvyizfj`, URL `https://vgsnjtqcuwrhifvyizfj.supabase.co`. Skripte 01, 02 und 03 sind erfolgreich im SQL-Editor gelaufen (Skript 04 noch nie). Drei Konten angelegt: moritz@, filipo@, leon@wessamedia.com (Auto Confirm). `.env` liegt lokal mit URL und Publishable key. Login aus der App hat funktioniert. Anleitung: `docs/supabase-einrichtung.md`. Selbstregistrierung ist ausgeschaltet (Authentication → Sign In / Providers → Abschnitt "User Signups" → "Allow new users to sign up" aus, erledigt am 8. September 2026).
- Testdaten-Beobachtung: Einzelne Wochen liegen unter 35 Stunden (z. B. Leon 23,7 h in KW 36), weil die Feierabend-Grenze 21 Uhr und Zufalls-Pausen die Tagesziele beschneiden. Bei Bedarf in Skript 3 Arbeitsbeginn früher legen oder Tagesziel begrenzen.
- Bedienung durch Claude: Seit 9. September 2026 erreicht die Chrome-Erweiterung (Claude in Chrome, "Browser 1") den Chrome des Auftraggebers, in dem er bei Supabase angemeldet ist. SQL-Skripte kann Claude damit selbst ausführen (Auftraggeber: "mach du das bitte für mich, ich gebe dir die Berechtigung"): SQL-Editor unter `/dashboard/project/vgsnjtqcuwrhifvyizfj/sql/new` öffnen, Cookies mit "Opt out" ablehnen, Skript per `window.monaco.editor.getModels()[0].setValue(sql)` einfügen, "Run" klicken; bei `drop` fragt Supabase "Potential issue detected" → "Run query". `select * from liga_stand()` liefert im Editor 0 Zeilen (auth.uid() ist dort null), Prüfung stattdessen über `pg_proc`. Vorher galt: Profil "Geschäftlich" nicht erreichbar, Dashboard nur per Screenshot und Klick-Anleitung. Das Electron-Fenster ist per computer-use bedienbar, aber nur wenn es gerade im Vordergrund ist; open_application "Electron" startet eine leere Electron-Demo, nicht die App.
- Datenschutz-Entscheidung im Schema: Blöcke sind nur für die eigene Person lesbar; das Team sieht Summen über die Funktion `team_stand(von, bis)`. Weicht vom Auftrag ("jeder darf alle Blöcke lesen") ab, Frage 7 war unbeantwortet. Umstellen = eine RLS-Policy ändern.
- Tätigkeitsnamen: Startregeln liefern direkt die Zielnamen mit "Learning" (Frage 10, unbeantwortet, Vorschlag umgesetzt).
- get-windows 9.3 bringt fertige Binärdateien für win32-x64 und darwin arm64/x64 mit; `require('get-windows')` funktioniert unter Node 24. Am 8. September 2026 auf dem Windows-PC erfolgreich getestet.
- Schritt 3 (automatische Erfassung): gebaut am 8. September 2026, unter Windows geprüft: Fenstererkennung, laufender Block, Pause/Fortsetzen, lokaler Speicher, Abgleich (1.030 Blöcke aus der Datenbank geholt). Vom Auftraggeber noch zu testen: Untätigkeit nach 3 Minuten, Schließen ins Symbol, Sperren/Ruhezustand, Symbol-Menü. Mac-Seite ungetestet.
  Bausteine: `src/main/erfassung.ts` (Takt 5 s, Untätigkeit rückwirkend, Lückenprüfung 30 s, Mitternacht- und 4-h-Teilung, Kurzblock-Regel 60 s, Inaktiv-Block max 60 min), `src/main/speicher.ts` (JSON je Konto unter `%APPDATA%\wessamedia Zeit\bloecke-<userId>.json`, atomar mit .bak), `src/main/sync.ts` (alle 60 s Upsert, Anfangsabgleich 13 Wochen mit Seiten à 1000), `src/main/tray.ts`, `src/main/programme.ts` (Namens-Tabelle), `src/shared/zeit.ts` (Berlin-Zeitrechnung ohne Zusatzpaket).
  Entscheidung: Ein Block endet auch bei Wechsel des Fenstertitels, nicht nur des Programms, sonst wären Titel-Regeln (Google Ads gegen YouTube im selben Chrome) wirkungslos; die 60-s-Regel hält die Liste ruhig. In der Entwicklungsversion heißt das eigene Fenster "Electron", in der gebauten App "wessamedia Zeit".
  Bewertung ist bis Schritt 5 immer "ungeklaert" (außer inaktiv), deshalb bleibt "Heute produktiv" vorerst 0,0.
- Schritt 4 (Heute-Screen mit echten Daten): gebaut am 8. September 2026. Tages-Ring (Recharts, vier Anteile inkl. ungeklärt), große Zahl mit `AnimierteZahl` (600 ms, respektiert "Bewegung reduzieren"), Blättern zu früheren Tagen (Pfeile, Klick auf das Datum springt zu heute), Ungeklärt-Hinweis mit Anzahl über alle Tage, Tagesliste (heute neueste zuerst, sonst chronologisch). Diagramme animieren nur beim ersten Aufbau. `DiagrammTooltip` ist der gemeinsame dunkle Tooltip für alle Diagramme. Leerer Schreibtisch (Windows Explorer ohne Fenstertitel) erzeugt keinen Block mehr.
- Schritt 5 (Regeln, Bewertung, Ungeklärt-Postfach, Nachbearbeitung): gebaut und am 8. September 2026 durchgespielt (Regel angelegt, 4 Blöcke sofort neu bewertet, Ring aktualisiert). Bausteine: `src/shared/regeln.ts` (Regelauswertung: persönlich vor Team, Programm vor Titel, Priorität, Musterlänge; `musterVorschlag` schneidet Browser-Anhang ab), `src/main/regelwerk.ts` (Regeln aus Supabase, Cache `regeln.json`, Neuladen alle 5 min), `src/main/bewertung.ts` (Neubewertung aller nicht geprüften Blöcke, auch nach jedem Abgleich aus der Datenbank), `src/main/bearbeiten.ts` (Ändern setzt manuellGeprueft, Löschen = geloeschtAm + Titel/Notiz leeren), `src/main/taetigkeiten.ts` (Schreibweise vereinheitlichen, neue Namen in Tabelle taetigkeit hochladen). Fenster: `BlockDialog` (auch fürs Durchgehen mit Fortschritt), `UngeklaertPostfach`, `Mehrfachleiste` (inkl. "Alle mit Programm X diese Woche"), `Hinweis` (Einblendung unten).
  Der laufende Block lässt sich nicht bearbeiten. Untätigkeit wird rückwirkend höchstens bis zum Start der Erfassung, zum Fortsetzen oder zum letzten Aufwachen gebucht (`zeitgrenze`), sonst entstehen überlappende Inaktiv-Blöcke bei Neustarts.
  Noch offen aus Schritt 5: "Woche durchgehen" mit auffälligen Blöcken (über 2 h, unbekanntes Programm) kommt mit dem Wochen-Screen in Schritt 6; Regeln bearbeiten/löschen kommt in den Einstellungen (Schritt 10).
- Schritt 6 (Ziele, Rang, Wochen-Screen): gebaut am 8. September 2026, am 9. September auf Ränge umgestellt. `src/shared/rang.ts` ist die eine Rang-Rechnung für alle (`rang`, `rangSchwelle`, `bisNaechsterRang`, `rangFortschritt`, `zielRang`, Tagesrichtwert = Gesamtziel/5, verbleibende Arbeitstage Mo–Fr inkl. heute); `level.ts` und `LevelRing.tsx` sind gelöscht, der Ring heißt `RangRing`. `src/main/ziele.ts` lädt alle Ziele (Cache `ziele.json`, Neuladen mit den Regeln alle 5 min). Fenster: `LevelRing` (SVG, ein Abschnitt je Level, Orange), `WochenBalken` (Recharts, vier Anteile, Richtwert-Linie), `TaetigkeitenListe` (Balken mit Zielmarke), `WocheScreen` (Blättern per Woche, Restlaufzeit-Satz, "Woche durchgehen" = ungeklärt oder über 2 h, nicht geprüft).
- Schritt 7 (Zeiten von Hand eintragen): gebaut am 9. September 2026. `eintragAnlegen` in `src/main/bearbeiten.ts` (quelle manuell, produktiv, manuellGeprueft, max. 24 h), IPC `bloecke:manuellAnlegen` und `bloecke:manuelleListe`. `EintragenScreen`: Datum/Von/Bis/Tätigkeit/Notiz, Dauer live, Überschneidungsprüfung mit automatischen Blöcken (300 ms verzögert) und Häkchen zum Löschen dieser Blöcke, "Speichern und nächsten" setzt Von auf das alte Bis. Darunter die letzten zehn Hand-Einträge, Klick öffnet den BlockDialog.
- Schritt 8 (Auswertungs-Screen, Symbole): gebaut am 9. September 2026. `src/shared/auswertung.ts` rechnet alles aus Blöcken: `produktivJeTag`, `gleitenderSchnitt` (7 Kalendertage), `hochrechnungBerechnen` (28 volle Tage vor heute, nur Tage ≥ 1 h produktiv, ×5, ×4,333, ×(52−Urlaubswochen), je Tätigkeit durch dieselbe Tageszahl), `wochenLevel` (12 abgeschlossene Wochen, leer = keine Blöcke), `verteilung` (Top 7 + Sonstige). `src/main/profil.ts` lädt urlaubswochen/idle_schwelle/fenstertitel_speichern und wendet sie auf die Erfassung an (IPC `profil:eigenes`, `profil:aendern`). `src/main/taetigkeiten.ts` kennt jetzt Symbole (IPC `taetigkeiten:symbole`, `symbolSetzen`).
  Fenster: `symbole.tsx` (SymbolProvider, `TaetigkeitSymbol`; kuratierte lucide-Liste `LUCIDE_SYMBOLE` mit deutschen Suchwörtern; Marken aus simple-icons in `MARKEN`; `MARKEN_ERSATZ` für Marken, die simple-icons nicht enthält: linkedin→Briefcase, adobepremierepro→Clapperboard, adobeaftereffects→Film, adobephotoshop→Image, capcut→Scissors, canva→Palette, slack→MessageSquare, openai→Bot; dunkle Marken tiktok/notion/anthropic/davinciresolve in Weiß), `MonatsVerlauf`, `TrendBalken`, `VerteilungsRing` (Grautöne, Hover orange, Legende mit Symbolen), `HochrechnungKarte`, `AuswertungScreen`. Symbole auch in `TaetigkeitenListe` und `BlockZeile`.
  Befund: Die installierte simple-icons-Version enthält KEIN LinkedIn, Premiere Pro, After Effects, Photoshop, Canva, Slack, OpenAI (die jsdelivr-Prüfung vom 8. September war irreführend). Ersatzsymbole greifen automatisch; eigene PNG-Logos wären ein späterer Ausbau.
- Nachbesserung 9. September 2026 auf Wunsch des Auftraggebers: Verteilungsring farbig (Markenfarbe, sonst `PALETTE`; Hover blendet die anderen aus) statt Grautöne. Fehlende Marken aus `simple-icons-13` (Alias auf simple-icons@13.21.0, nur intern): LinkedIn, Premiere Pro, After Effects, Photoshop, Lightroom, Canva, Slack, OpenAI; CapCut gibt es dort nicht (Schere bleibt). Zeitraum-Wähler auf der Auswertung (7 Tage, 30 Tage, 12 Wochen, 6 Monate, 12 Monate, gemerkt in localStorage): Verlauf, Trend (mind. 12 Wochen) und Verteilung folgen ihm, Hochrechnung bleibt bei 28 Tagen. Datenquelle: `bloecke:tagesSummen` → bis 90 Tage zurück lokal (`tagessummenAusBloecken` in `src/shared/summen.ts`), davor RPC `tages_summen` aus `supabase/05_auswertung.sql`. Skript 5 muss der Auftraggeber noch ausführen; bis dahin zeigen 6 und 12 Monate einen Hinweis.
- Schritt 9 (Team-Screen): gebaut am 9. September 2026. IPC `team:stand` ruft die Datenbankfunktion `team_stand(von, bis)` für die laufende Woche auf (nur Summen, keine Blöcke der anderen) und ersetzt die eigene Zeile durch lokale Live-Werte. `TeamScreen`: Karten mit Initialen, Level, Stunden gegen das persönliche Gesamtziel (aus `ziele:alle`), Balken, "zuletzt gemeldet"; sortiert nach Level, dann Stunden; `TeamBalken` mit Ziel-Linie (eigener Balken weiß, andere grau, ab Ziel grün). Aktualisierung alle 60 s.
- Schritt 10 (Einstellungen): gebaut am 9. September 2026. `EinstellungenScreen` mit Abschnitten Erfassung (Untätigkeit in Minuten, Fenstertitel-Schalter, Autostart nur in der installierten App), Wochenziele (Gesamt + je Tätigkeit, anlegen/löschen; IPC `ziele:setzen`/`ziele:loeschen`, Upsert auf user_id+taetigkeit), Regeln (zweizeilig, jede Änderung speichert sofort und bewertet neu; IPC `regeln:aendern`/`regeln:loeschen`; persönliche Regeln anderer werden nicht angezeigt), Tätigkeiten und Symbole (`SymbolWahl` mit Suche über Marken und kuratierte lucide-Liste), Hochrechnung (Urlaubswochen), System (Rechte-Hinweis, Datenbank-Abgleich, Version über `system:info`), Konto (Abmelden). Zahlenfelder speichern beim Verlassen oder mit Enter.
- Schritt 11 (Auszeichnungen, Wochenzusammenfassung, Rang-Aufstieg): gebaut am 9. September 2026. `src/shared/auszeichnungen.ts` (`wochenstatistik`, `zieleErreicht`, `auszeichnungenPruefen` über die letzten 12 Wochen; "Erster Champion", "Serie" und "Alle Lernziele" sofort, "Fokus-Woche" nur für abgeschlossene Wochen mit ≤ 2 h unproduktiv ab Rang 5). `src/main/auszeichnungen.ts` speichert neue in der Tabelle `auszeichnung` (testdaten-Flag wird übernommen); Prüfung nach dem Regel-Laden, alle 10 min und 30 s nach Blockänderungen; neue gehen als `auszeichnungen:neu` ans Fenster (Einblendung unten). `src/main/feier.ts` merkt sich je Woche den gefeierten Rang (`feier-<uid>.json`) und ob die Sonntags-Meldung schon kam; `ErfassungsStatus.neuerRang` steuert die Einblendung `RangAufstieg` (nur bei sichtbarem Fenster, 4 s oder Klick, danach `rang:gefeiert`). Sonntags ab 18 Uhr einmal eine Electron-Notification. Fenster: `Wochenzusammenfassung` (oben auf der Woche: für jede vergangene Woche beim Blättern, montags die Vorwoche, sonntags ab 18 Uhr als Zwischenstand), `Auszeichnungen` + `AuszeichnungBild` (Sechseck mit lucide-Symbol als Platzhalter, bis PNGs unter `src/assets/badges/` liegen). Beim ersten Start wird der aktuelle Wochenrang einmal gefeiert (kein gefeierter Stand vorhanden), danach nur echte Aufstiege.
- Auszeichnungen erweitert (9. September 2026, "mehr coole Auszeichnungen"): 14 Typen, alle in `src/shared/auszeichnungen.ts` (`AUSZEICHNUNGEN` mit Titel, Text, Farbe; `Wochenstatistik` kennt jetzt `tage[0..6]`, `frueh` (vor 8 Uhr), `spaet` (nach 22 Uhr), `laengsteStrecke` (produktiv am Stück, Lücken ≤ 5 min)); neue: dauerbrenner (4 Wochen ≥ Rang 5), comeback (< Rang 5 → ≥ Zielrang), eternal (Rang 15), perfekte_woche (Mo–Fr je ≥ 8 h), marathon (10 h an einem Tag), sprint (3 h am Stück), fruehaufsteher (2 h vor 8), nachteule (2 h nach 22), wochenend_krieger (5 h Sa+So), aufgeraeumt (abgeschlossene Woche ≥ 20 h ohne ungeklärt). DB-Check-Constraint per `supabase/11_auszeichnungen.sql` erweitern (sonst schlagen Inserts fehl). `AuszeichnungBild` ist eine Medaille mit Ordensband in der Auszeichnungsfarbe (wappen-Bausteine) und lucide-Symbol; Raster 7 pro Zeile.
- Wappen-Stil (9. September 2026, dritte Runde, "richtig cooler Spielstyle"; seit der vierten Runde mit KI-Bildern nur noch für Schrift, Band, Sterne und `AuszeichnungBild` im Einsatz): `wappen.tsx` bold: Sockel (Form 3,5 nach unten in Dunkel), dicke dunkle Kontur (3,2), vertikaler satter Metallverlauf, farbiges statt schwarzes Feld, Glanz oben (Form auf 70 %/40 % gestaucht mit Weißverlauf), Lichtkante, Schrift 900 mit doppelter Kontur und Schattenkante, goldene Sterne mit dunkler Kontur, alle Stufen leuchten (`-leuchten` immer definiert). Hintergrund (Stand nach Rückmeldung vom 9. September 2026: "Nebelflecken, Radar und Halbkreis-Bogen raus, Streifen nicht cool"): Raster, zwei sehr dezente Zifferblatt-Ringe ohne farbige Bögen, 44 Lichtpunkte und drei geschwungene LICHTBAHNEN (`BAHNEN` in `Hintergrund.tsx`, SVG viewBox 1000 × 600 mit preserveAspectRatio none, von unten links nach oben rechts): je Bahn eine kaum sichtbare Linie plus zwei `.hintergrund-bahn`-Pfade (farbiger Schein 7 px mit drop-shadow, weißer Kern 2 px), auf denen ein 160 Einheiten langer Funke per animiertem `stroke-dashoffset` (2560 → 320, 7–11 s, alternate) hin- und herzischt. Lichtkugeln, Radar und Sternschnuppen wieder entfernt.
- Schritt 12 (Installer, README): gebaut am 9. September 2026. `electron-builder.yml` (NSIS-Installer ohne Adminrechte, deutsche Sprache, Desktop-Verknüpfung; Mac als universelles DMG; `npmRebuild: false`; `asarUnpack` für get-windows; Electron, node-gyp, node-addon-api und darwin-.node-Dateien ausgeschlossen). `build/icon.png` kommt aus `scripts/icon-erzeugen.mjs` (`npm run icon`), electron-builder macht daraus .ico und .icns. Skripte: `npm run build:win`, `build:mac`, `build:mac:app`, `build:unpack`; `scripts\installer-windows.cmd` für den Auftraggeber. GitHub-Ablauf `.github/workflows/installer.yml` (Run workflow oder Tag `v*`): Windows-Job und Mac-Job (`npm ci --ignore-scripts`, Electron nachladen, `chmod +x` für `get-windows/main`, `--dir`-Bau, Ad-hoc-`codesign --deep --sign -`, DMG per `--prepackaged`), Ergebnisse als Artifacts und bei Tags als Release. Zugangsdaten kommen aus den Repository-Secrets `VITE_SUPABASE_URL` und `VITE_SUPABASE_PUBLISHABLE_KEY`. Windows-Installer am 9. September 2026 lokal gebaut (113 MB, asar 15 MB) und die gepackte App geprüft (Oberfläche, Preload, Erfassung, Abgleich, CSP). Mac-Bau noch nie gelaufen; erster Lauf auf GitHub steht aus. README.md geschrieben (Installation mit Warnungen, Bedienung, Supabase-Konten, Bauen, Datenablage).
  Entscheidungen dabei: CSP nicht mehr per onHeadersReceived, sondern als Meta-Tag, das `electron.vite.config.ts` nur beim Bauen in die index.html einfügt (in `out/renderer/index.html` prüfbar). Nur `get-windows` und `@supabase/supabase-js` sind `dependencies` (werden extern geladen), alles andere `devDependencies` und damit gebündelt; sonst zieht `@electron-toolkit/*` über peerDependencies ganz Electron ins Paket. Autostart wird nur beim allerersten Start der gepackten App eingeschaltet (Marke `autostart-eingerichtet` im Datenordner), danach gilt der Schalter in den Einstellungen. `--devtools` beim Start öffnet die Entwicklerwerkzeuge auch in der gepackten App.
  Nachtrag 9. September 2026: Der Anfangsabgleich entfernt lokale Blöcke, die die Datenbank im 13-Wochen-Fenster nicht mehr kennt (`verwaisteEntfernen`; ausstehende und Blöcke der letzten 10 Minuten bleiben). So verschwinden die Testdaten nach Skript 4 beim nächsten App-Start auch aus dem Zwischenspeicher. Die Auszeichnungsprüfung wartet auf Regeln und ersten Abgleich (`sync.erstAbgleich`).
  Eigenheiten: Eine `files`-Liste unter `win:` oder `mac:` ersetzt die globale Liste komplett (dann landen .git, dist und .env im Paket). Die gepackte App ist für computer-use nicht freigegeben, Screenshots zeigen nur schwarze Rechtecke; prüfen geht mit `--remote-debugging-port=9222` und einem kleinen CDP-Skript (Runtime.evaluate auf document.body.innerText). Beim Testen der gepackten App entstehen ein Run-Eintrag in der Registry und die Autostart-Marke; beides danach entfernen. `taskkill /IM` scheitert in der Git-Bash an der Pfadumwandlung, stattdessen PowerShell `Stop-Process -Name 'wessamedia Zeit'`.

## Befehle
- `npm run dev` startet die App zum Entwickeln. Unter Windows ohne PATH: `scripts\dev-windows.cmd`.
- `npm run build` baut die App (ohne Installer).
- `npm run typecheck` prüft die Typen.
- Vorschau im Browserfenster: `preview_start` mit "dev" (Startdatei liegt in `.claude/launch.json`; die Kopie im alten OneDrive-Ordner zeigt auf `scripts/dev-windows.cmd`). Der Renderer läuft dann auch unter http://localhost:5173 ohne Electron-APIs.

## Bekannte Eigenheiten
- `npm install` hat die Electron-Programmdatei nicht heruntergeladen (Postinstall lief nicht). Abhilfe: `node node_modules/electron/install.js`.
- Das Renderer-Bundle ist rund 570 kB, davon fast alles React. Nur die verwendeten lucide-Symbole landen darin.
